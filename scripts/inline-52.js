
(function(root,factory){
  const api=factory();
  if(typeof module!=='undefined'&&module.exports)module.exports=api;
  root.MTGEngine=api;
})(typeof window!=='undefined'?window:globalThis,function(){
  const VERSION='phase4-rules-scale-explain-1';
  const SNAPSHOT_LIMIT=60;
  const EVENT_LIMIT=2000;

  function uid(prefix='evt'){
    if(globalThis.crypto&&typeof globalThis.crypto.randomUUID==='function')return prefix+'_'+globalThis.crypto.randomUUID();
    return prefix+'_'+Date.now().toString(36)+'_'+Math.random().toString(36).slice(2,10);
  }
  function clone(value){return JSON.parse(JSON.stringify(value));}
  function commanderKey(playerId,commanderId){return commanderId||('player_'+playerId+'_commander_1');}
  function asBigInt(value,fallback=0n){try{return BigInt(String(value??fallback).replace(/,/g,''));}catch{return fallback;}}
  function exactAdd(a,b){const out=asBigInt(a)+asBigInt(b);return (out<=BigInt(Number.MAX_SAFE_INTEGER)&&out>=BigInt(Number.MIN_SAFE_INTEGER))?Number(out):out.toString();}
  function exactNonNegativeAdd(a,b){const out=asBigInt(a)+asBigInt(b);const safe=out<0n?0n:out;return safe<=BigInt(Number.MAX_SAFE_INTEGER)?Number(safe):safe.toString();}
  function formatExact(v){return asBigInt(v).toLocaleString('en-US');}

  function ensureMeta(state){
    state.meta??={};
    state.meta.engineVersion=VERSION;
    state.meta.seq??=0;
    state.meta.matchId??=uid('match');
    state.events??=[];state.snapshots??=[];state.feed??=[];
    state.calculations??=[];
    state.stats??={players:{}};
    state.matchFinalization??=null;
    return state;
  }

  function normalizeStackItem(item,index=0){
    item.id??=uid('stack');
    item.kind??='item';
    item.name??='Unnamed stack object';
    item.controllerId??=item.idController??item.controller??item.owner??item.playerId??null;
    item.sourceObjectId??=item.sourceId??null;
    item.targets=Array.isArray(item.targets)?item.targets:[];
    item.count=String(item.count??'1');
    item.order??=index;
    item.status??='pending';
    item.linkGroupId=item.linkGroupId?String(item.linkGroupId):null;
    item.sourceIncarnationId=item.sourceIncarnationId?String(item.sourceIncarnationId):null;
    item.linkKey=item.linkKey?String(item.linkKey):null;if(typeof MTGFaceDownMechanics!=='undefined')MTGFaceDownMechanics.normalizePublicState(item);
    return item;
  }
  function markControlledSinceTurn(o,state){if(o&&state)o.controlledSinceTurn=Number(state.turn||1);return o}
  function normalizePermanent(o,index=0){
    o.id??=uid(o.token?'cohort':'obj');
    if(typeof MTGLKI!=='undefined')MTGLKI.normalizeIdentity(o);
    o.owner=Number(o.owner??o.controllerId??0)||o.owner;
    o.controllerId??=o.owner;
    o.q=String(o.q??'1');
    o.damageMarked=String(o.damageMarked??'0');
    o.deathtouchMarked=!!o.deathtouchMarked;
    o.combatMeta??={};
    const normCombatStat=v=>{if(v===null||v===undefined||String(v).trim()==='')return null;try{return BigInt(String(v).replace(/,/g,'').trim()).toString()}catch{return null}};
    o.combatMeta.power=normCombatStat(o.combatMeta.power??o.power);
    o.combatMeta.toughness=normCombatStat(o.combatMeta.toughness??o.toughness);
    o.combatMeta.traits??={};
    o.tapped=!!o.tapped;if(typeof MTGPhasing!=='undefined')MTGPhasing.normalizeObject(o);if(typeof MTGCardForms!=='undefined')MTGCardForms.normalizeObject(o);if(typeof MTGFaceDownMechanics!=='undefined')MTGFaceDownMechanics.normalizePublicState(o);
    o.c??={};o.sub??=[];o.colors=Array.isArray(o.colors)?o.colors:[];o.continuousEffects=Array.isArray(o.continuousEffects)?o.continuousEffects:[];o.replacementEffects=Array.isArray(o.replacementEffects)?o.replacementEffects:[];
    if(o.token)o.cohortId??=o.id;
    o.order??=index;
    return o;
  }
  function normalizeState(state){
    ensureMeta(state);
    state.life??={};state.poison??={};state.tax??={};state.cmdCasts??={};state.energy??={};state.exp??={};state.libraryCounts??={};
    state.cmd??={};state.commanders??={};state.commanderCasting??={byPlayer:{}};state.commanderCasting.byPlayer??={};state.commanderZoneChoices??={byObject:{}};state.commanderZoneChoices.byObject??={};state.choice??={};state.fallen??={};state.stack??=[];state.battle??=[];state.zones??={};for(const z of ['graveyard','exile','hand','command'])state.zones[z]??=[];state.targeting??={sets:{}};state.targeting.sets??={};state.attachments??={links:{},pendingAuraSba:{}};state.attachments.links??={};if(!state.attachments.pendingAuraSba||typeof state.attachments.pendingAuraSba!=='object'||Array.isArray(state.attachments.pendingAuraSba))state.attachments.pendingAuraSba={};state.combat??={active:false,hadAttackersThisCombat:false,attackers:[],blockers:[],damageAssignments:[],damageResolved:false};state.combat.active=!!state.combat.active;state.combat.hadAttackersThisCombat=!!state.combat.hadAttackersThisCombat;state.combat.attackers=Array.isArray(state.combat.attackers)?state.combat.attackers:[];state.combat.blockers=Array.isArray(state.combat.blockers)?state.combat.blockers:[];state.combat.damageAssignments=Array.isArray(state.combat.damageAssignments)?state.combat.damageAssignments:[];state.combat.damageResolved=!!state.combat.damageResolved;state.combat.damageEffects=Array.isArray(state.combat.damageEffects)?state.combat.damageEffects:[];state.combatSteps??={active:false,current:'idle',firstStepRequired:false,completed:[],resolvedAssignmentIds:[],sequence:0,firstSnapshot:{captured:false,sources:{}}};state.combatSteps.active=!!state.combatSteps.active;state.combatSteps.current=String(state.combatSteps.current||'idle');state.combatSteps.firstStepRequired=!!state.combatSteps.firstStepRequired;state.combatSteps.completed=Array.isArray(state.combatSteps.completed)?state.combatSteps.completed:[];state.combatSteps.resolvedAssignmentIds=Array.isArray(state.combatSteps.resolvedAssignmentIds)?state.combatSteps.resolvedAssignmentIds.map(String):[];state.combatSteps.sequence=Number(state.combatSteps.sequence||0);state.combatSteps.firstSnapshot=typeof MTGAdvancedCombatDamage!=='undefined'?MTGAdvancedCombatDamage.normalizeSnapshot(state.combatSteps.firstSnapshot):((state.combatSteps.firstSnapshot&&typeof state.combatSteps.firstSnapshot==='object')?state.combatSteps.firstSnapshot:{captured:false,sources:{}});
    state.continuousEffects=Array.isArray(state.continuousEffects)?state.continuousEffects:[];state.replacementEffects=Array.isArray(state.replacementEffects)?state.replacementEffects:[];state.rulesReview=Array.isArray(state.rulesReview)?state.rulesReview:[];
    state.triggerOrderPending??=null;state.oracleChoicePending??=null;state.replacementOrderPending??=null;state.castingCostPending??=null;state.lastCastingCostByPlayer??={};state.advancedResolutionPending??=null;state.lastAdvancedResolutionByPlayer??={};state.cleanupLoop=typeof MTGCleanupLoop!=='undefined'?MTGCleanupLoop.normalize(state.cleanupLoop||{}):(state.cleanupLoop||{iteration:0,collecting:false,exceptional:false,priorityGranted:false,repeatRequired:false,lastReason:null,lastCheckpointSeq:0});state.smartPriority??={mode:'standard',stopProfiles:{}};state.smartPriority.mode=['relaxed','standard','full'].includes(String(state.smartPriority.mode||'').toLowerCase())?String(state.smartPriority.mode).toLowerCase():'standard';state.smartPriority.stopProfiles??={};for(const [pid,rawStops] of Object.entries(state.smartPriority.stopProfiles||{})){let s=rawStops&&typeof rawStops==='object'?rawStops:{};state.smartPriority.stopProfiles[pid]={main1:!!s.main1,combat:!!s.combat,main2:!!s.main2,end:!!s.end}};
    state.priorityFlow??={windowId:null,mode:state.smartPriority.mode,reason:null,sourceStackId:null,order:[],holder:null,passed:[],consecutivePassCount:0,complete:false};
    state.priorityFlow.mode=['relaxed','standard','full'].includes(String(state.priorityFlow.mode||'').toLowerCase())?String(state.priorityFlow.mode).toLowerCase():state.smartPriority.mode;
    state.priorityFlow.order=Array.isArray(state.priorityFlow.order)?state.priorityFlow.order.map(Number).filter(Boolean):[];state.priorityFlow.passed=Array.isArray(state.priorityFlow.passed)?state.priorityFlow.passed.map(Number).filter(Boolean):[];state.priorityFlow.holder=state.priorityFlow.holder==null?null:Number(state.priorityFlow.holder);state.priorityFlow.consecutivePassCount=Math.max(0,Number(state.priorityFlow.consecutivePassCount||0));state.priorityFlow.complete=!!state.priorityFlow.complete;
    state.priority??=state.priorityFlow.holder??state.respond??null;if(state.priorityFlow.windowId&&state.priorityFlow.holder!==state.priority)state.priorityFlow.holder=state.priority;
    if(typeof MTGLinkedAbilities!=='undefined')MTGLinkedAbilities.normalizeState(state);if(typeof MTGCastPermissions!=='undefined')MTGCastPermissions.normalizeState(state);if(typeof MTGPreparedExile!=='undefined')MTGPreparedExile.normalizeState(state);if(typeof MTGSuspend!=='undefined')MTGSuspend.normalizeState(state);if(typeof MTGAlternateZoneCasting!=='undefined')MTGAlternateZoneCasting.normalizeState(state);
    state.stack.forEach(x=>{normalizeStackItem(x);if(typeof MTGLinkedAbilities!=='undefined')MTGLinkedAbilities.bindStackItem(state,x)});state.battle.forEach(normalizePermanent);for(const z of ['graveyard','exile','hand','command'])for(const o of state.zones[z]||[]){if(typeof MTGLKI!=='undefined')MTGLKI.normalizeIdentity(o);if(typeof MTGCardForms!=='undefined')MTGCardForms.normalizeObject(o);if(typeof MTGFaceDownMechanics!=='undefined')MTGFaceDownMechanics.normalizePublicState(o)}
    return state;
  }

  function registerCommander(state,playerId,name,commanderId){
    normalizeState(state);state.commanders[playerId]??=[];
    let list=state.commanders[playerId], existing=list.find(c=>c.id===commanderId)||(name&&list.find(c=>c.name===name));
    if(existing){if(name)existing.name=name;return existing;}
    const c={id:commanderId||commanderKey(playerId),ownerId:Number(playerId),name:name||'Commander'};list.push(c);return c;
  }

  function makeEvent(state,type,payload={},actorId=null,meta={}){
    normalizeState(state);
    return {id:uid('evt'),seq:(state.meta.seq||0)+1,type,actorId,payload:clone(payload),at:Date.now(),clientId:meta.clientId||null,source:meta.source||'ui',atomicBatchId:meta.atomicBatchId||payload?.atomicBatchId||null,version:3};
  }
  function appendFeed(state,text){if(text)state.feed.push(text);}
  function untapOracle(o){return String(o?.oracleText||o?.oracle_text||o?.phase100CardPublic?.oracleText||'').replace(/[’]/g,"'").replace(/\r/g,'').trim();}
  function untapTypeMatch(o,kind){let type=String(o?.typeLine||o?.type||'');kind=String(kind||'').toLowerCase();if(kind==='permanent'||kind==='permanents')return true;if(kind==='nonland permanent'||kind==='nonland permanents')return !/\bLand\b/i.test(type);if(/^creature/.test(kind))return /\bCreature\b/i.test(type);if(/^artifact/.test(kind))return /\bArtifact\b/i.test(type);if(/^land/.test(kind))return /\bLand\b/i.test(type);return false;}
  function untapSourceEnabled(o,txt){let name=String(o?.name||o?.n||'').trim();if(!name)return true;let esc=name.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');if(new RegExp('as long as '+esc+' is untapped','i').test(txt)&&o.tapped)return false;if(new RegExp('as long as '+esc+' is tapped','i').test(txt)&&!o.tapped)return false;return true;}
  function untapPolicy(state,activePlayer){activePlayer=Number(activePlayer);let blocked=new Set(),reasons=[],manual=[],skipStep=false,battle=(state?.battle||[]).filter(o=>!(typeof MTGPhasing!=='undefined'&&MTGPhasing.isPhasedOut?.(o))),controlled=battle.filter(o=>Number(o.controllerId??o.owner??0)===activePlayer);for(const src of battle){let txt=untapOracle(src);if(!txt||!/untap step/i.test(txt)||!untapSourceEnabled(src,txt))continue;let ctrl=Number(src.controllerId??src.owner??0),lines=txt.split(/\n|(?<=\.)\s+/).map(x=>x.trim()).filter(Boolean);for(const line of lines){let lower=line.toLowerCase(),handled=false,affectsByRelation=!/\byou control\b/i.test(line)||ctrl===activePlayer;affectsByRelation=affectsByRelation&&(!/\byour opponents?\b/i.test(line)||ctrl!==activePlayer);
      if(/\bplayers? skip (?:their|the) untap steps?\b/i.test(line)||ctrl===activePlayer&&/\byou skip your untap step\b/i.test(line)||ctrl!==activePlayer&&/\byour opponents? skip (?:their|the) untap steps?\b/i.test(line)){skipStep=true;reasons.push(String(src.name||src.id||'Public effect'));handled=true;continue}
      let name=String(src.name||src.n||'').trim();if(name&&ctrl===activePlayer){let esc=name.replace(/[.*+?^${}()|[\]\\]/g,'\\$&'),selfRe=new RegExp("(?:^|\\n|\\.\\s*)"+esc+" (?:doesn't|does not) untap during (?:your|its controller's) (?:next )?untap step",'i');if(selfRe.test(txt)){blocked.add(String(src.id));reasons.push(name);handled=true;}}
      let ench=line.match(/^Enchanted (?:creature|permanent) (?:doesn't|does not) untap during its controller's (?:next )?untap step/i);if(ench){let tid=state?.attachments?.links?.[String(src.id)],target=controlled.find(o=>String(o.id)===String(tid||''));if(target){blocked.add(String(target.id));reasons.push(String(src.name||src.id||'Attachment'));}handled=true;continue}
      let m=line.match(/^(Artifacts?|Creatures?|Lands?|Permanents?|Nonland permanents?)(?: (your opponents control|you control))? (?:don't|do not) untap during (?:their controllers'|your) untap steps?/i);if(m){let rel=String(m[2]||'').toLowerCase();if((!rel||rel==='you control'&&ctrl===activePlayer||rel==='your opponents control'&&ctrl!==activePlayer)){for(const o of controlled)if(untapTypeMatch(o,m[1]))blocked.add(String(o.id));reasons.push(String(src.name||src.id||'Public effect'));}handled=true;continue}
      if(affectsByRelation&&(/\b(?:can't|cannot) untap more than\b/i.test(line)||/\b(?:doesn't|does not|don't|do not) untap during .*untap step/i.test(line)||/\buntap (?:only|no more than)\b/i.test(line))&&!handled)manual.push({sourceId:String(src.id||''),name:String(src.name||src.id||'Public effect'),text:line.slice(0,500)});
    }}return{skipStep,blockedIds:[...blocked],manualReview:manual.length>0,manual,reasons:[...new Set(reasons)]};}
  function stunCounterRemovalBlocked(state,o){let controller=Number(o?.controllerId??o?.owner??0);for(const src of state?.battle||[]){if(typeof MTGPhasing!=='undefined'&&MTGPhasing.isPhasedOut?.(src))continue;let txt=untapOracle(src);if(!txt||!untapSourceEnabled(src,txt))continue;let ctrl=Number(src.controllerId??src.owner??0),global=/\b(?:stun )?counters? can(?:not|'t) be removed from permanents\b/i.test(txt),yours=/\b(?:stun )?counters? can(?:not|'t) be removed from permanents you control\b/i.test(txt),opps=/\b(?:stun )?counters? can(?:not|'t) be removed from permanents your opponents control\b/i.test(txt);if(global||yours&&ctrl===controller||opps&&ctrl&&ctrl!==controller)return{blocked:true,sourceId:String(src.id||''),name:String(src.name||src.id||'Public effect'),text:txt}}return{blocked:false}}
  function ensurePlayerStats(state,id){state.stats??={players:{}};state.stats.players??={};return state.stats.players[id]??={damageDealt:0,commanderDamageDealt:0,lifeGained:0,lifeLost:0,tokensCreated:'0'};}
  function recordCalculation(state,event,title,steps,result,kind='rules'){
    const c={id:'calc_'+event.id,eventId:event.id,seq:event.seq,title,kind,steps:steps.map(String),result:String(result),at:event.at};
    state.calculations.push(c);if(state.calculations.length>250)state.calculations.shift();return c;
  }


  function damageTargetKey(t){return t?.kind==='player'?'p:'+Number(t.playerId):t?.kind==='object'?'o:'+String(t.objectId||''):'';}
  function damageEffectMatches(e,a){const sc=e?.scope||{};if(sc.sourceObjectId&&String(sc.sourceObjectId)!==String(a?.sourceObjectId))return false;if(sc.target&&damageTargetKey(sc.target)!==damageTargetKey(a?.target))return false;return true;}
  function validDamageTarget(state,t){if(t?.kind==='player')return Number(t.playerId)>0;if(t?.kind==='object')return !!String(t.objectId||'')&&state.battle.some(o=>String(o.id)===String(t.objectId));return false;}
  function applyDamageEffects(state,a){let amount=asBigInt(a?.amount),target=clone(a?.target||null),used=[];const list=(state.combat.damageEffects||[]).slice().sort((x,y)=>Number(x.order||0)-Number(y.order||0));for(const e of list){if(e.used||!damageEffectMatches(e,a))continue;if(e.kind==='redirect'&&validDamageTarget(state,e.toTarget)){target=clone(e.toTarget);used.push({id:e.effectId,kind:'redirect'});continue;}if(e.kind==='prevent'){let remaining=asBigInt(e.remainingAmount??e.amount);if(remaining<=0n)continue;let take=remaining<amount?remaining:amount;if(take>0n){amount-=take;used.push({id:e.effectId,kind:'prevent',amount:take});}}}return{amount,target,used};}
  function consumeDamageEffects(state,used){for(const u of used||[]){const e=(state.combat.damageEffects||[]).find(x=>String(x.effectId)===String(u.id));if(!e)continue;if(u.kind==='prevent'){let left=asBigInt(e.remainingAmount??e.amount)-asBigInt(u.amount);e.remainingAmount=(left<0n?0n:left).toString();if(e.oneShot!==false||left<=0n)e.used=true;}else if(u.kind==='redirect'&&e.oneShot!==false)e.used=true;}}
  function recordRulesReview(state,event,kind,detail,playerId=null){state.rulesReview??=[];const key=String(event?.id||event?.seq||Date.now())+':'+String(kind||'rules');if(state.rulesReview.some(x=>x.key===key))return;state.rulesReview.push({key,eventId:event?.id||null,seq:event?.seq||null,kind:String(kind||'rules'),detail:String(detail||'Manual rules review required.'),playerId:playerId==null?null:Number(playerId),resolved:false,createdAt:event?.at||Date.now()});if(state.rulesReview.length>100)state.rulesReview.splice(0,state.rulesReview.length-100);appendFeed(state,'RULES REVIEW · '+String(detail||kind||'Manual review required.'));}


  function objectOwnerId(o){return Number(o?.owner??o?.ownerId??o?.commanderOwnerId??0)||0;}
  function objectControllerId(o){return Number(o?.controllerId??o?.controller??o?.playerId??objectOwnerId(o))||0;}
  function unlinkObjectAttachments(state,objectId,event,reason='object-left'){const rid=String(objectId||'');if(!rid)return;state.attachments??={links:{},pendingAuraSba:{}};state.attachments.links??={};state.attachments.pendingAuraSba??={};delete state.attachments.pendingAuraSba[rid];delete state.attachments.links[rid];for(const [src,tgt] of Object.entries(state.attachments.links)){if(String(tgt)!==rid)continue;if(typeof MTGProtectionRules!=='undefined'&&MTGProtectionRules.noteAttachmentEnded)MTGProtectionRules.noteAttachmentEnded(state,src,rid,Number(event?.seq||state.meta?.seq||0),reason);delete state.attachments.links[src]}}
  function unlinkRemovedAttachments(state,removedIds=new Set(),event,reason='objects-left'){const removed=new Set([...removedIds].map(String));state.attachments??={links:{},pendingAuraSba:{}};state.attachments.links??={};state.attachments.pendingAuraSba??={};for(const [src,tgt] of Object.entries(state.attachments.links)){if(removed.has(String(src))){delete state.attachments.pendingAuraSba[String(src)];delete state.attachments.links[src];continue}if(removed.has(String(tgt))){if(typeof MTGProtectionRules!=='undefined'&&MTGProtectionRules.noteAttachmentEnded)MTGProtectionRules.noteAttachmentEnded(state,src,tgt,Number(event?.seq||state.meta?.seq||0),reason);delete state.attachments.links[src]}}for(const rid of removed)delete state.attachments.pendingAuraSba[rid]}
  function cleanupDepartedObjectReferences(state,removedIds= new Set(),changedControllerIds=new Set(),event=null){
    const removed=new Set([...removedIds].map(String)),changed=new Set([...changedControllerIds].map(String)),departedFromCombat=id=>removed.has(String(id||''))||changed.has(String(id||''));
    unlinkRemovedAttachments(state,removed,event,'player-left');
    state.targeting??={sets:{}};state.targeting.sets??={};
    for(const [key,set] of Object.entries(state.targeting.sets)){if(removed.has(String(set?.sourceObjectId||''))){delete state.targeting.sets[key];continue;}set.targets=(set.targets||[]).filter(t=>!(t?.kind==='object'&&removed.has(String(t.objectId||''))));}
    state.combat??={active:false,hadAttackersThisCombat:false,attackers:[],blockers:[],damageAssignments:[],damageResolved:false};
    state.combat.attackers=(state.combat.attackers||[]).filter(a=>!departedFromCombat(a.objectId));
    state.combat.blockers=(state.combat.blockers||[]).filter(b=>!departedFromCombat(b.blockerObjectId));
    state.combat.damageAssignments=(state.combat.damageAssignments||[]).filter(a=>!departedFromCombat(a.sourceObjectId)&&!(a.target?.kind==='object'&&departedFromCombat(a.target.objectId)));
    state.combat.damageEffects=(state.combat.damageEffects||[]).filter(e=>!departedFromCombat(e?.scope?.sourceObjectId??e?.sourceObjectId)&&!(e.target?.kind==='object'&&departedFromCombat(e.target.objectId))&&!(e.toTarget?.kind==='object'&&departedFromCombat(e.toTarget.objectId)));
    if(!state.combat.damageAssignments.length)state.combat.damageResolved=false;
    if(!state.combat.attackers.length&&!state.combat.blockers.length)state.combat.active=false;
    state.continuousEffects=(state.continuousEffects||[]).filter(e=>!removed.has(String(e.sourceObjectId||'')));
    state.replacementEffects=(state.replacementEffects||[]).filter(e=>!removed.has(String(e.sourceObjectId||'')));
    state.commanderZoneChoices??={byObject:{}};state.commanderZoneChoices.byObject??={};for(const id of removed)delete state.commanderZoneChoices.byObject[id];
  }
  function applyPlayerLeavesGame(state,target,event,payload={}){
    target=Number(target);if(!target)return{removedObjectIds:[],removedStackIds:[],revertedControlIds:[],exiledControlIds:[]};
    const removedIds=new Set(),changedControllerIds=new Set(),exiledControlIds=[];
    const priorBattle=state.battle||[];
    state.battle=priorBattle.filter(o=>{if(objectOwnerId(o)===target){removedIds.add(String(o.id||''));return false;}return true;});

    // CR 800.4a: tracked control-changing effects that give the departing player control end first.
    state.continuousEffects=(state.continuousEffects||[]).filter(e=>{
      const kind=String(e?.kind||'').toLowerCase(),to=Number(e?.controllerId??e?.toControllerId??e?.setControllerId??0);
      return !(kind.includes('control')&&to===target);
    });

    for(const o of state.battle.slice()){
      if(objectControllerId(o)!==target)continue;
      const owner=objectOwnerId(o);
      if(owner&&owner!==target&&!state.fallen?.[owner]){
        o.controllerId=owner;o.controlledSinceTurn=Number(state.turn||1);changedControllerIds.add(String(o.id||''));
      }else{
        removedIds.add(String(o.id||''));exiledControlIds.push(String(o.id||''));
      }
    }
    if(exiledControlIds.length)state.battle=state.battle.filter(o=>!removedIds.has(String(o.id||'')));

    for(const z of ['graveyard','exile','hand','command'])state.zones[z]=(state.zones[z]||[]).filter(o=>{if(objectOwnerId(o)===target){removedIds.add(String(o.id||''));return false;}return true;});

    const removedStackIds=[];
    state.stack=(state.stack||[]).filter(item=>{
      const representedOwner=objectOwnerId(item?.phase100CardObject||item?.publicCard||item),controller=objectControllerId(item);
      if(controller===target||representedOwner===target){removedStackIds.push(String(item.id||''));return false;}return true;
    });

    cleanupDepartedObjectReferences(state,removedIds,changedControllerIds,event);
    for(const set of Object.values(state.targeting?.sets||{}))set.targets=(set.targets||[]).filter(t=>!(t?.kind==='player'&&Number(t.playerId)===target));
    state.combat.damageAssignments=(state.combat.damageAssignments||[]).filter(a=>!(a.target?.kind==='player'&&Number(a.target.playerId)===target));
    state.combat.damageEffects=(state.combat.damageEffects||[]).filter(e=>!(e.target?.kind==='player'&&Number(e.target.playerId)===target)&&!(e.toTarget?.kind==='player'&&Number(e.toTarget.playerId)===target));
    if(!state.combat.damageAssignments.length)state.combat.damageResolved=false;

    const stackIds=new Set((state.stack||[]).map(x=>String(x.id||'')));
    if(state.oracleChoicePending&&(Number(state.oracleChoicePending.playerId)===target||(state.oracleChoicePending.stackItemId&&!stackIds.has(String(state.oracleChoicePending.stackItemId)))))state.oracleChoicePending=null;
    if(state.castingCostPending&&Number(state.castingCostPending.playerId)===target)state.castingCostPending=null;
    if(state.advancedResolutionPending&&Number(state.advancedResolutionPending.playerId)===target)state.advancedResolutionPending=null;
    if(state.replacementOrderPending&&Number(state.replacementOrderPending.playerId)===target)state.replacementOrderPending=null;
    if(state.triggerOrderPending){
      state.triggerOrderPending.apnapOrder=(state.triggerOrderPending.apnapOrder||[]).map(Number).filter(pid=>pid!==target);
      state.triggerOrderPending.groups=(state.triggerOrderPending.groups||[]).filter(g=>Number(g?.controllerId)!==target);
      if(!state.triggerOrderPending.groups.length)state.triggerOrderPending=null;
    }

    // Any control change removes that permanent from combat under CR 506.4; reference cleanup above enforces it.
    const successor=Number(payload.designationSuccessorId||0)||null;
    if(Number(state.mon||0)===target)state.mon=successor;
    if(Number(state.ini||0)===target)state.ini=successor;

    event.playerLeaveCleanup={removedObjectIds:[...removedIds].filter(Boolean),removedStackIds,revertedControlIds:[...changedControllerIds].filter(Boolean),exiledControlIds};
    return event.playerLeaveCleanup;
  }

  function commanderIdentityForObject(o){if(!o||!(o.isCommander||o.commanderId))return null;const owner=Number(o.commanderOwnerId??o.owner);if(!owner)return null;return{ownerId:owner,commanderId:String(o.commanderId||commanderKey(owner)),name:String(o.commanderName||o.name||'Commander')};}
  function recordCommanderZoneChoice(state,o,to,event){to=String(to||'').toLowerCase();const ident=commanderIdentityForObject(o),objectId=String(o?.id||'');if(!ident||!objectId||(to!=='graveyard'&&to!=='exile'))return null;state.commanderZoneChoices??={byObject:{}};state.commanderZoneChoices.byObject??={};const choice={objectId,ownerId:ident.ownerId,commanderId:ident.commanderId,name:ident.name,toZone:to,createdAt:event?.at||Date.now()};state.commanderZoneChoices.byObject[objectId]=choice;appendFeed(state,ident.name+' may be moved to the command zone.');return choice;}
  function recordCommanderCombatDamage(state,sourceObject,defenderId,amount){const ident=commanderIdentityForObject(sourceObject),n=asBigInt(amount);if(!ident||!Number(defenderId)||n<=0n)return false;registerCommander(state,ident.ownerId,ident.name,ident.commanderId);state.cmd[defenderId]??={};const before=asBigInt(state.cmd[defenderId][ident.commanderId]||0),after=before+n;state.cmd[defenderId][ident.commanderId]=after<=BigInt(Number.MAX_SAFE_INTEGER)?Number(after):after.toString();const safe=n<=BigInt(Number.MAX_SAFE_INTEGER)?Number(n):Number.MAX_SAFE_INTEGER;ensurePlayerStats(state,ident.ownerId).commanderDamageDealt+=safe;appendFeed(state,ident.name+' dealt '+formatExact(n)+' commander combat damage to Player '+defenderId+'.');return true;}

  function phase94ResolveCombatBatch(state,event,assignments,{step=null,strict=true}={}){
    const list=(assignments||[]).filter(a=>!step||String(a.flags?.step||'')===String(step));
    let audit=typeof MTGAdvancedCombatDamage!=='undefined'?MTGAdvancedCombatDamage.auditAssignments(state,step,list):{ok:true,review:false,reasons:[],reviewReasons:[]};event.phase94AssignmentAudit=clone(audit);
    if(strict&&!audit.ok){let why=(audit.reasons||[]);recordRulesReview(state,event,'combat-assignment-review','Combat damage assignment is incomplete or illegal'+(why.length?': '+why.join('; '):'.'),event.actorId);event.phase94ResolutionBlocked=true;appendFeed(state,'COMBAT DAMAGE · Resolution paused for illegal or incomplete assignment.');return{ok:false,resolvedIds:[]};}if(strict&&audit.review&&!event.payload?.manualVerified){let why=(audit.reviewReasons||[]);recordRulesReview(state,event,'combat-assignment-review','Combat damage assignment requires physical/manual verification'+(why.length?': '+why.join('; '):'.'),event.actorId);event.phase94ResolutionBlocked=true;appendFeed(state,'COMBAT DAMAGE · Resolution paused for manual verification.');return{ok:false,resolvedIds:[]};}if(audit.review&&event.payload?.manualVerified){event.phase94ManualVerified=true;appendFeed(state,'COMBAT DAMAGE · Manual physical-table verification accepted.');}
    const damageEvents=[],lifeBySource=new Map(),resolvedIds=[];
    for(const a of list){let applied=applyDamageEffects(state,a),amount=applied.amount,target=applied.target;if(amount<0n)amount=0n;consumeDamageEffects(state,applied.used);const src=state.battle.find(o=>String(o.id)===String(a.sourceObjectId||'')),srcTraits=src?(typeof MTGContinuousEffects!=='undefined'?MTGContinuousEffects.derived(state,src).traits:(src.combatMeta?.traits||{})):{};let excessPre=typeof MTGAdvancedCombatDamage!=='undefined'?MTGAdvancedCombatDamage.excessForAssignment(state,a):{known:false,excess:'0'};
      if(typeof MTGProtectionRules!=='undefined'&&amount>0n){let pr=MTGProtectionRules.preventDamage(state,{sourceObjectId:String(a.sourceObjectId||''),sourceControllerId:Number(src?.controllerId??src?.owner??0),target,amount:amount.toString(),flags:{combat:true,cantBePrevented:!!a.flags?.cantBePrevented}});if(pr.review)recordRulesReview(state,event,'combat-protection-review','Protection damage prevention could not be proven safely.',target?.kind==='player'?Number(target.playerId):null);amount=asBigInt(pr.amount);}
      if(typeof MTGReplacementEffects!=='undefined'&&amount>0n){let rr=MTGReplacementEffects.applyDamage(state,{affectedPlayerId:target?.kind==='player'?Number(target.playerId):0,targetObjectId:target?.kind==='object'?String(target.objectId):null,sourceObjectId:String(a.sourceObjectId||''),amount:amount.toString()});if(rr.reviewRequired)recordRulesReview(state,event,'combat-damage-replacement','Combat damage replacement/prevention ordering requires manual review.',target?.kind==='player'?Number(target.playerId):null);amount=asBigInt(rr.value);MTGReplacementEffects.consume(state,rr.consume||[]);}
      if(amount<=0n){resolvedIds.push(String(a.assignmentId));damageEvents.push({assignmentId:String(a.assignmentId),sourceObjectId:String(a.sourceObjectId),target:clone(target),amount:'0',prevented:true});continue;}
      const n=amount<=BigInt(Number.MAX_SAFE_INTEGER)?Number(amount):Number.MAX_SAFE_INTEGER,sourceController=Number(src?.controllerId??src?.owner??0);if(target?.kind==='player'&&Number(target.playerId)){const pid=Number(target.playerId);state.life[pid]=exactAdd(state.life[pid]??40,-amount);const ps=ensurePlayerStats(state,pid);ps.lifeLost=(ps.lifeLost||0)+n;if(sourceController){const os=ensurePlayerStats(state,sourceController);os.damageDealt=(os.damageDealt||0)+n;}recordCommanderCombatDamage(state,src,pid,amount);}else if(target?.kind==='object'){const o=state.battle.find(x=>String(x.id)===String(target.objectId));if(o){o.damageMarked=exactNonNegativeAdd(o.damageMarked||'0',amount.toString());if(srcTraits.deathtouch)o.deathtouchMarked=true;}}
      if(srcTraits.lifelink&&sourceController){let k=String(a.sourceObjectId),g=lifeBySource.get(k)||{sourceObjectId:k,controllerId:sourceController,amount:0n};g.amount+=amount;lifeBySource.set(k,g);}damageEvents.push({assignmentId:String(a.assignmentId),sourceObjectId:String(a.sourceObjectId),sourceControllerId:sourceController,target:clone(target),amount:amount.toString(),deathtouch:!!srcTraits.deathtouch,lifelink:!!srcTraits.lifelink,trample:!!srcTraits.trample,excess:excessPre.known?String(excessPre.excess):null});resolvedIds.push(String(a.assignmentId));}
    const gains=[];for(const g of lifeBySource.values()){let gain=g.amount;if(typeof MTGReplacementEffects!=='undefined'){let lr=MTGReplacementEffects.applyLifeGain(state,{affectedPlayerId:g.controllerId,amount:gain.toString()});if(lr.reviewRequired)recordRulesReview(state,event,'lifelink-replacement','Lifelink life-gain replacement requires manual review.',g.controllerId);gain=asBigInt(lr.value);}state.life[g.controllerId]=exactAdd(state.life[g.controllerId]??40,gain);const ls=ensurePlayerStats(state,g.controllerId);ls.lifeGained=(ls.lifeGained||0)+(gain<=BigInt(Number.MAX_SAFE_INTEGER)?Number(gain):Number.MAX_SAFE_INTEGER);gains.push({sourceObjectId:g.sourceObjectId,playerId:g.controllerId,amount:gain.toString()});}
    event.phase94DamageEvents=damageEvents;event.phase94LifeGainEvents=gains;event.phase94ResolutionBlocked=false;return{ok:true,resolvedIds};
  }

  function reduce(state,event){
    normalizeState(state);const p=event.payload||{},id=event.actorId;
    switch(event.type){
      case 'LIFE_CHANGED':{
        const target=p.playerId??id;let d=Number(p.delta||0),repl=null;if(d>0&&typeof MTGReplacementEffects!=='undefined'){repl=MTGReplacementEffects.applyLifeGain(state,{affectedPlayerId:Number(target),amount:String(d)});if(repl.reviewRequired)recordRulesReview(state,event,'life-gain-replacement','Life-gain replacement ordering/effect requires manual review.',target);d=Number(repl.value);}
        state.life[target]=exactAdd(state.life[target]??40,d);const ps=ensurePlayerStats(state,target);if(d>0)ps.lifeGained+=d;else ps.lifeLost+=Math.abs(d);
        appendFeed(state,'Player '+target+' '+(d<0?'lost ':'gained ')+Math.abs(d)+' life.'+(repl?.applied?.length?' Replacement effect applied.':''));break;
      }
      case 'LIFE_CHANGED_EXACT':{
        const target=p.playerId??id;let delta=asBigInt(p.delta),original=delta,repl=null;if(delta>0n&&typeof MTGReplacementEffects!=='undefined'){repl=MTGReplacementEffects.applyLifeGain(state,{affectedPlayerId:Number(target),amount:delta.toString()});if(repl.reviewRequired)recordRulesReview(state,event,'life-gain-replacement','Life-gain replacement ordering/effect requires manual review.',target);delta=asBigInt(repl.value);}
        state.life[target]=exactAdd(state.life[target]??40,delta);appendFeed(state,'Player '+target+' life '+(delta>=0n?'+':'')+formatExact(delta)+'.');
        recordCalculation(state,event,'Exact life change',['Requested change: '+(original>=0n?'+':'')+formatExact(original),...(repl?.applied||[]).map(x=>'Replacement ×'+x.multiplier),'Applied change: '+(delta>=0n?'+':'')+formatExact(delta)],'Final life: '+formatExact(state.life[target]),'life');break;
      }
      case 'LIFE_LOST':{
        const target=p.playerId??p.targetPlayerId??id,amount=Math.max(0,Number(p.amount||0));if(!target||!amount)break;const before=state.life[target]??40;state.life[target]=exactAdd(before,-amount);ensurePlayerStats(state,target).lifeLost+=amount;appendFeed(state,'Player '+target+' lost '+amount+' life.');recordCalculation(state,event,'Life loss',['Life before: '+formatExact(before),'Life loss: '+amount], 'Final life: '+formatExact(state.life[target]),'life');break;
      }
      case 'DAMAGE_DEALT':{
        const requestedAmount=Math.max(0,Number(p.amount||0));let phase87Applied=[];
        if(typeof MTGReplacementOrdering!=='undefined'&&!p._phase87ReplacementResolved&&requestedAmount>0){let ctx=MTGReplacementOrdering.contextFor('DAMAGE_DEALT',p),plan=ctx?MTGReplacementOrdering.pending(state,'DAMAGE_DEALT',p,ctx,Number(ctx.affectedPlayerId||id)):null;if(plan?.review?.length)recordRulesReview(state,event,'replacement-order-review','A replacement/prevention effect could not be safely ordered automatically.',ctx?.affectedPlayerId);if(plan?.pending){plan.pending.actorId=id;plan.pending.source=event.source;state.replacementOrderPending=plan.pending;event.replacementDeferred=true;appendFeed(state,'REPLACEMENT ORDER · Player '+plan.pending.playerId+' must choose which applicable effect to apply next.');break}if(plan){Object.assign(p,MTGReplacementOrdering.payloadFrom('DAMAGE_DEALT',p,plan.ctx));phase87Applied=plan.applied||[];event.phase87ReplacementApplied=phase87Applied.slice();}}
        const defender=p.defenderId??p.targetPlayerId??id,sourcePlayer=Number(p.sourcePlayerId??id);let amount=Math.max(0,Number(p.amount||0)),baseAmount=requestedAmount,repl=null,protection=null;if(!defender||!amount){p.amount=String(amount);if(amount===0)appendFeed(state,'Damage was fully prevented.');break}if(typeof MTGProtectionRules!=='undefined'){protection=MTGProtectionRules.preventDamage(state,{sourceObjectId:p.sourceObjectId||null,sourceControllerId:sourcePlayer,target:{kind:'player',playerId:Number(defender)},amount:String(amount),flags:{combat:String(p.damageType||'').toLowerCase()==='combat',cantBePrevented:!!p.cantBePrevented}});if(protection.review)recordRulesReview(state,event,'protection-damage-review','Protection damage prevention could not be proven safely.',defender);amount=Math.max(0,Number(protection.amount));}if(typeof MTGReplacementEffects!=='undefined'&&!p._phase87ReplacementResolved&&amount>0){repl=MTGReplacementEffects.applyDamage(state,{affectedPlayerId:Number(defender),sourcePlayerId:sourcePlayer,amount:String(amount)});if(repl.reviewRequired)recordRulesReview(state,event,'damage-replacement','Damage replacement/prevention ordering requires manual review.',defender);amount=Math.max(0,Number(repl.value));MTGReplacementEffects.consume(state,repl.consume||[]);}p.amount=String(amount);if(!amount){appendFeed(state,'Damage to Player '+defender+' was fully prevented.');break;}
        const before=state.life[defender]??40;state.life[defender]=exactAdd(before,-amount);
        const ps=ensurePlayerStats(state,sourcePlayer);ps.damageDealt+=amount;ensurePlayerStats(state,defender).lifeLost+=amount;
        const steps=['Base damage: '+baseAmount,...phase87Applied.map(x=>'CR 616 applied: '+x),...(protection?.applied?['Protection prevented '+protection.prevented+' damage'+(protection.quality?' from '+protection.quality:'')]:[]),...(repl?.applied||[]).map(x=>'Prevented by '+x.effectId+': '+x.prevented),'Applied damage: '+amount,'Player '+defender+' life: '+formatExact(before)+' → '+formatExact(state.life[defender])];
        if(p.isCommander){
          const sourceKey=commanderKey(sourcePlayer,p.sourceCommanderId);state.cmd[defender]??={};state.cmd[defender][sourceKey]=(state.cmd[defender][sourceKey]||0)+amount;ps.commanderDamageDealt+=amount;
          steps.push('Commander source '+sourceKey+': '+(state.cmd[defender][sourceKey]-amount)+' → '+state.cmd[defender][sourceKey]);
        }
        appendFeed(state,'Player '+sourcePlayer+' dealt '+amount+(p.isCommander?' commander':'')+' '+(p.damageType||'')+' damage to Player '+defender+'.'.replace('  ',' '));
        recordCalculation(state,event,p.isCommander?'Commander damage':'Damage',['Type: '+(p.damageType||'damage'),...steps],amount+' damage','damage');break;
      }
      case 'COUNTER_CHANGED':{
        const target=p.playerId??id,k=p.counter,delta=Number(p.delta||0);if(!k)break;state[k]??={};state[k][target]=Math.max(0,(Number(state[k][target]||0))+delta);if(k==='cmdCasts')state.tax[target]=Math.max(0,(state.cmdCasts[target]-1)*2);break;
      }
      case 'STATUS_TOGGLED':{const k=p.status,target=p.playerId??id;if(k)state[k]=state[k]===target?null:target;break;}
      case 'COMMANDER_REGISTERED':{registerCommander(state,p.playerId??id,p.name,p.commanderId);break;}
      case 'COMMANDER_DAMAGE_CHANGED':{
        const defender=p.defenderId??id,sourcePlayer=p.sourcePlayerId,delta=Number(p.delta||0),sourceKey=commanderKey(sourcePlayer,p.sourceCommanderId);
        if(sourcePlayer==null)break;state.cmd[defender]??={};state.cmd[defender][sourceKey]=Math.max(0,(state.cmd[defender][sourceKey]||0)+delta);
        appendFeed(state,'Player '+defender+' commander damage '+(delta>=0?'+':'')+delta+' from Player '+sourcePlayer+'.');break;
      }
      case 'COMMANDER_DAMAGE_DEALT':{
        // Compatibility event from Phase 3. New callers should use DAMAGE_DEALT.
        const defender=p.defenderId??id,sourcePlayer=Number(p.sourcePlayerId),amount=Math.max(0,Number(p.amount||0)),sourceKey=commanderKey(sourcePlayer,p.sourceCommanderId);
        if(!sourcePlayer||!amount)break;state.life[defender]=(Number(state.life[defender]??40))-amount;state.cmd[defender]??={};state.cmd[defender][sourceKey]=(state.cmd[defender][sourceKey]||0)+amount;
        appendFeed(state,'Player '+sourcePlayer+' dealt '+amount+' commander damage to Player '+defender+'.');break;
      }
      case 'COMMANDER_CAST_RECORDED':{
        const target=p.playerId??id,c=registerCommander(state,target,p.name,p.commanderId),cid=String(p.commanderId||c.id||commanderKey(target)),fromZone=String(p.fromZone||'command').toLowerCase();
        state.cmdCasts[target]=(state.cmdCasts[target]||0)+1;state.tax[target]=Math.max(0,(state.cmdCasts[target]-1)*2);
        state.commanderCasting??={byPlayer:{}};state.commanderCasting.byPlayer??={};const pk=String(Number(target)||target);state.commanderCasting.byPlayer[pk]??={};let ce=state.commanderCasting.byPlayer[pk][cid];if(!ce)ce=state.commanderCasting.byPlayer[pk][cid]={commanderId:cid,playerId:Number(target),name:String(p.name||c.name||'Commander'),castsFromCommand:0,lastCastTax:0,nextTax:0,lastFromZone:null};ce.name=String(p.name||ce.name||'Commander');ce.lastFromZone=fromZone;if(fromZone==='command'){ce.lastCastTax=Math.max(0,Number(ce.castsFromCommand||0)*2);ce.castsFromCommand=Math.max(0,Number(ce.castsFromCommand||0))+1;ce.nextTax=ce.castsFromCommand*2;}
        appendFeed(state,'Player '+target+' cast commander '+(p.name||'Commander')+(fromZone==='command'?' from the command zone (tax '+ce.lastCastTax+', next '+ce.nextTax+').':' from '+fromZone+'.'));break;
      }
      case 'LIBRARY_SETUP_CONFIRMED':{
        const target=Number(p.playerId??id),deckSize=Math.max(0,Number(p.deckSize||100)),commanderCount=Math.max(0,Number(p.commanderCount||0)),mulligans=Math.max(0,Number(p.mulligans||0)),multiplayer=p.multiplayer!==false,startingHand=Math.max(0,Number(p.startingHand||7)),bottom=multiplayer?Math.max(0,mulligans-1):mulligans,openingHand=Math.max(0,startingHand-bottom),value=Math.max(0,deckSize-commanderCount-openingHand);
        state.libraryCounts[target]={value,exact:true,confirmed:true,source:'opening-setup',deckSize,commanderCount,mulligans,openingHand,draws:0,manualAdjustments:0};appendFeed(state,'Player '+target+' confirmed opening library: '+value+' cards after mulligans.');break;
      }
      case 'LIBRARY_COUNT_CHANGED':{
        const target=Number(p.playerId??id),delta=Math.trunc(Number(p.delta||0));if(!target||!Number.isFinite(delta))break;let r=state.libraryCounts[target];if(!r||typeof r!=='object')r=state.libraryCounts[target]={value:Math.max(0,Number(r||0)),exact:false,confirmed:false,source:'manual'};r.value=Math.max(0,Number(r.value||0)+delta);r.manualAdjustments=Math.max(0,Number(r.manualAdjustments||0)+1);if(p.reason==='draw')r.draws=Math.max(0,Number(r.draws||0)+Math.max(0,-delta));if(p.exact===false)r.exact=false;if(p.exact===true)r.exact=true;appendFeed(state,'Player '+target+' library '+(delta>=0?'+':'')+delta+(p.reason?' · '+p.reason:'')+'.');break;
      }
      case 'LIBRARY_COUNT_SET':{
        const target=Number(p.playerId??id),value=Math.max(0,Math.trunc(Number(p.value||0)));if(!target||!Number.isFinite(value))break;let prev=state.libraryCounts[target]&&typeof state.libraryCounts[target]==='object'?state.libraryCounts[target]:{};state.libraryCounts[target]={...prev,value,exact:p.exact!==false,confirmed:true,source:'physical-count',manualAdjustments:Number(prev.manualAdjustments||0)+1};appendFeed(state,'Player '+target+' physically confirmed '+value+' cards in library.');break;
      }
      case 'TURN_ADVANCED':{
        let order=(p.playerOrder||[]).map(id=>({id:Number(id)})),plan=p.repeatCleanup?{phase:'CLEANUP',active:Number(state.active||1),turn:Number(state.turn||1),newTurn:false}:typeof MTGTurnStructure!=='undefined'?MTGTurnStructure.next(state,order,{skipDrawStep:!!p.skipDrawStep,drawSkipReason:p.drawSkipReason||null}):null,staticUntapSkips=[],crossedNewTurn=!!plan?.newTurn;
        if(plan&&!p.repeatCleanup&&typeof MTGTurnStructure!=='undefined'){let guard=0;while(plan&&String(plan.phase||'').toUpperCase()==='UNTAP'&&guard++<32){let up=untapPolicy(state,plan.active);if(!up.skipStep)break;staticUntapSkips.push({playerId:Number(plan.active),policy:up});if(plan.topology)state.turnTopology=plan.topology;state.phase=plan.phase;state.active=plan.active;state.turn=plan.turn;let after=MTGTurnStructure.next(state,order,{});if(!after)break;crossedNewTurn=crossedNewTurn||!!after.newTurn;plan=after}if(plan&&staticUntapSkips.length){plan.newTurn=crossedNewTurn;plan.skipped=[...staticUntapSkips.map(()=> 'UNTAP'),...(plan.skipped||[])];plan.skipDetails=[...staticUntapSkips.map(x=>({id:'UNTAP',reason:'effect',playerId:x.playerId})),...(plan.skipDetails||[])];}}
        if(plan){if(plan.topology)state.turnTopology=plan.topology;state.phase=plan.phase;state.active=plan.active;state.turn=plan.turn;if(staticUntapSkips.length)appendFeed(state,'Player '+staticUntapSkips.at(-1).playerId+' skipped '+(staticUntapSkips.length===1?'the untap step.':staticUntapSkips.length+' untap steps.')+' Tracked public effect: '+(staticUntapSkips.at(-1).policy.reasons.join(', ')||'untap-step restriction')+'.');if((plan.skipped||[]).includes('DRAW'))appendFeed(state,plan.drawSkipReason==='two-player-first-turn'?'Starting player skipped the first draw step in a two-player game.':'Player '+state.active+' skipped the draw step because of a tracked effect.');if((plan.skipped||[]).includes('DECLARE BLOCKERS'))appendFeed(state,'No attackers were declared · blockers and combat damage were skipped.')}
        if(state.phase!=='CLEANUP'&&typeof MTGCleanupLoop!=='undefined')state.cleanupLoop=MTGCleanupLoop.clearRepeat(state.cleanupLoop||{});
        if(state.phase==='BEGIN COMBAT'){state.combat.active=false;state.combat.hadAttackersThisCombat=false;state.combat.attackers=[];state.combat.blockers=[];state.combat.damageAssignments=[];state.combat.damageResolved=false;state.combat.actionGate=null;state.combatSteps.active=false;state.combatSteps.current='idle';state.combatSteps.completed=[];state.combatSteps.resolvedAssignmentIds=[];}if(state.phase==='UNTAP'){if(typeof MTGPhasing!=='undefined'){event.phase96Phasing=MTGPhasing.applyUntapStep(state,state.active,event);if(event.phase96Phasing.phasedIn.length||event.phase96Phasing.phasedOut.length)appendFeed(state,'PHASING · '+event.phase96Phasing.phasedIn.length+' phased in · '+event.phase96Phasing.phasedOut.length+' phased out.')}let up=untapPolicy(state,state.active),stunReplaced=[],blocked=new Set(up.blockedIds||[]);state.untapReview=up.manualReview?{playerId:Number(state.active),turn:Number(state.turn||1),sources:up.manual.map(x=>({sourceId:x.sourceId,name:x.name,text:x.text})),reason:'public-untap-restriction'}:null;if(!up.manualReview){for(const o of state.battle){if(Number(o.controllerId??o.owner)!==Number(state.active)||(typeof MTGPhasing!=='undefined'&&MTGPhasing.isPhasedOut(o))||!o.tapped||blocked.has(String(o.id)))continue;let counters=o.c&&typeof o.c==='object'?o.c:{},stunKey=Object.keys(counters).find(k=>String(k).trim().toLowerCase()==='stun'),stun=stunKey?asBigInt(counters[stunKey]||0):0n;if(stun>0n){let removal=stunCounterRemovalBlocked(state,o);if(!removal.blocked)counters[stunKey]=String(stun-1n);stunReplaced.push(String(o.name||o.id||'Permanent')+(removal.blocked?' (counter retained)':''));continue}o.tapped=false}}appendFeed(state,'Player '+state.active+' began the untap step.'+(blocked.size?' '+blocked.size+' tracked permanent'+(blocked.size===1?' stayed':'s stayed')+' tapped because of public untap restrictions.':'')+(up.manualReview?' UNTAP REVIEW · a public effect limits or conditions what may untap; tracked tapped permanents were left unchanged for physical confirmation.':'')+(stunReplaced.length?' STUN · '+stunReplaced.length+' untap'+(stunReplaced.length===1?' was':'s were')+' replaced.':''));}
        if(state.phase!=='DRAW')state.drawReview=null;if(state.phase==='DRAW'){const activeInGame=!state.fallen?.[Number(state.active)],r=state.libraryCounts?.[state.active];event.turnDrawPerformed=false;state.drawReview=null;if(!activeInGame)event.turnDrawSuppressedReason='active-player-left-game';else if(p.drawReviewRequired){let sources=(Array.isArray(p.drawReviewSources)?p.drawReviewSources:[]).map(x=>({sourceId:String(x?.sourceId||''),name:String(x?.name||'Replacement effect'),text:String(x?.text||''),reason:String(x?.reason||'draw-replacement')}));state.drawReview={playerId:Number(state.active),turn:Number(state.turn||1),sources,reason:'public-draw-replacement'};event.turnDrawSuppressedReason='replacement-review';appendFeed(state,'DRAW REVIEW · Player '+state.active+' has a public effect that may replace the turn draw. Automatic library decrement was withheld for physical confirmation.')}else if(p.drawPrevented){event.turnDrawSuppressedReason='draw-prohibited';appendFeed(state,'Player '+state.active+' did not draw for turn because a tracked public effect says that player can’t draw cards.')}else if(r&&typeof r==='object'&&r.confirmed!==false&&p.autoDraw!==false){r.value=Math.max(0,Number(r.value||0)-1);r.draws=Math.max(0,Number(r.draws||0)+1);event.turnDrawPerformed=true;appendFeed(state,'Player '+state.active+' drew for turn. Library: '+r.value+'.')}}
        if(state.phase==='CLEANUP'){if(typeof MTGContinuousEffects!=='undefined')MTGContinuousEffects.expireTemporary(state);if(typeof MTGReplacementEffects!=='undefined')MTGReplacementEffects.expireTemporary(state);for(const o of state.battle){o.damageMarked='0';o.deathtouchMarked=false}state.combat.active=false;state.combat.hadAttackersThisCombat=false;state.combat.attackers=[];state.combat.blockers=[];state.combat.damageAssignments=[];state.combat.damageResolved=false;state.combat.actionGate=null;state.combatSteps.active=false;state.combatSteps.current='idle';state.combatSteps.completed=[];state.combatSteps.resolvedAssignmentIds=[];appendFeed(state,'Cleanup step began. Tracked marked damage and combat state cleared.');}
        break;
      }
      case 'CONTINUOUS_EFFECT_ADDED':{let e=clone(p.effect||{});e.id=String(e.id||uid('conteff'));e.timestamp=Number(e.timestamp??event.seq);state.continuousEffects=state.continuousEffects.filter(x=>String(x.id)!==e.id);state.continuousEffects.push(e);appendFeed(state,'Temporary continuous effect added.');break;}
      case 'CONTINUOUS_EFFECT_REMOVED':{state.continuousEffects=state.continuousEffects.filter(x=>String(x.id)!==String(p.effectId||''));appendFeed(state,'Continuous effect removed.');break;}
      case 'CONTINUOUS_EFFECTS_EXPIRED':{let n=typeof MTGContinuousEffects!=='undefined'?MTGContinuousEffects.expireTemporary(state):0;if(n)appendFeed(state,n+' until-end-of-turn continuous effect'+(n===1?' expired.':'s expired.'));break;}
      case 'REPLACEMENT_EFFECT_ADDED':{let e=clone(p.effect||{});e.id=String(e.id||uid('repleff'));e.timestamp=Number(e.timestamp??event.seq);state.replacementEffects=state.replacementEffects.filter(x=>String(x.id)!==e.id);state.replacementEffects.push(e);appendFeed(state,'Replacement/prevention effect added.');break;}
      case 'REPLACEMENT_EFFECT_REMOVED':{state.replacementEffects=state.replacementEffects.filter(x=>String(x.id)!==String(p.effectId||''));appendFeed(state,'Replacement/prevention effect removed.');break;}
      case 'REPLACEMENT_EFFECTS_EXPIRED':{let n=typeof MTGReplacementEffects!=='undefined'?MTGReplacementEffects.expireTemporary(state):0;if(n)appendFeed(state,n+' until-end-of-turn replacement/prevention effect'+(n===1?' expired.':'s expired.'));break;}
      case 'CLEANUP_LOOP_UPDATED':{state.cleanupLoop=typeof MTGCleanupLoop!=='undefined'?MTGCleanupLoop.normalize({...state.cleanupLoop,...(p.patch||{})}):{...state.cleanupLoop,...clone(p.patch||{})};if(p.note)appendFeed(state,String(p.note));break;}
      case 'SELECTION_SET':{const target=p.playerId??id,key=String(p.key||'choice'),value=p.value;state.choice[target]??={};if(typeof state.choice[target]!=='object'||Array.isArray(state.choice[target]))state.choice[target]={legacy:state.choice[target]};state.choice[target][key]=value;appendFeed(state,'Player '+target+' set '+key+' to '+String(value)+'.');break;}
      case 'TARGET_SET_CHANGED':{const sid=String(p.setId||'manual'),seen=new Set(),targets=[];for(const t of (Array.isArray(p.targets)?p.targets:[])){let n=null;if(String(t?.kind).toLowerCase()==='player'&&Number(t.playerId))n={kind:'player',playerId:Number(t.playerId)};if(String(t?.kind).toLowerCase()==='object'&&String(t.objectId||'')&&state.battle.some(o=>String(o.id)===String(t.objectId)&&!(typeof MTGPhasing!=='undefined'&&MTGPhasing.isPhasedOut(o))))n={kind:'object',objectId:String(t.objectId)};if(!n)continue;const k=n.kind==='player'?'p:'+n.playerId:'o:'+n.objectId;if(!seen.has(k)){seen.add(k);targets.push(n)}}state.targeting.sets[sid]={id:sid,targets,sourceObjectId:p.sourceObjectId?String(p.sourceObjectId):null,sourcePlayerId:p.sourcePlayerId==null?null:Number(p.sourcePlayerId),updatedAt:event.at};appendFeed(state,'Targets updated: '+targets.length+'.');break;}
      case 'TARGET_SET_CLEARED':{delete state.targeting.sets[String(p.setId||'manual')];appendFeed(state,'Targets cleared.');break;}
      case 'OBJECT_ATTACHED':{
        const sourceId=String(p.sourceObjectId||''),targetId=String(p.targetObjectId||'');if(!sourceId||!targetId||sourceId===targetId)break;
        if(!state.battle.some(o=>String(o.id)===sourceId&&!(typeof MTGPhasing!=='undefined'&&MTGPhasing.isPhasedOut(o)))||!state.battle.some(o=>String(o.id)===targetId&&!(typeof MTGPhasing!=='undefined'&&MTGPhasing.isPhasedOut(o))))break;
        const attachSource=state.battle.find(o=>String(o.id)===sourceId),attachTarget=state.battle.find(o=>String(o.id)===targetId);if(typeof MTGProtectionRules!=='undefined'){let al=MTGProtectionRules.attachmentLegality(state,attachSource,attachTarget);if(!al.legal){appendFeed(state,(attachSource?.name||'Object')+' cannot attach to '+(attachTarget?.name||'that permanent')+(al.review?' · rules review required.':' because of protection.'));break;}}
        let cur=targetId,seen=new Set(),cycle=false;while(cur){if(cur===sourceId){cycle=true;break;}if(seen.has(cur)){cycle=true;break;}seen.add(cur);cur=state.attachments.links[cur]||'';}if(cycle)break;
        state.attachments.links[sourceId]=targetId;if(typeof MTGProtectionRules!=='undefined'&&MTGProtectionRules.clearPendingAura)MTGProtectionRules.clearPendingAura(state,sourceId);const so=state.battle.find(o=>String(o.id)===sourceId),to=state.battle.find(o=>String(o.id)===targetId);appendFeed(state,(so?.name||'Object')+' attached to '+(to?.name||'object')+'.');break;}
      case 'OBJECT_DETACHED':{const sourceId=String(p.sourceObjectId||'');if(!sourceId)break;const targetId=state.attachments.links[sourceId];if(targetId){if(typeof MTGProtectionRules!=='undefined'&&MTGProtectionRules.noteAttachmentEnded)MTGProtectionRules.noteAttachmentEnded(state,sourceId,targetId,Number(event.seq||0),'detached');delete state.attachments.links[sourceId];const so=state.battle.find(o=>String(o.id)===sourceId);appendFeed(state,(so?.name||'Object')+' detached.');}break;}
      case 'COMBAT_ACTION_GATE_OPENED':{let pending=[...new Set((p.pending||[]).map(Number).filter(x=>x&&!state.fallen?.[x]))];state.combat.actionGate={phase:String(p.phase||state.phase||''),kind:String(p.kind||''),step:p.step==null?null:String(p.step),pending,completed:[],complete:pending.length===0,openedAtSeq:Number(event.seq||0)};appendFeed(state,String(p.label||'Combat turn-based action')+(p.step?' · '+String(p.step).toUpperCase()+' STEP':'')+(pending.length?' · waiting for P'+pending.join(', P')+'.':' · no player decisions required.'));break;}
      case 'COMBAT_ACTION_GATE_COMPLETED':{let g=state.combat.actionGate,pid=Number(p.playerId??id);if(!g||String(g.phase||'').toUpperCase()!==String(state.phase||'').toUpperCase()||(p.step!=null&&String(g.step||'')!==String(p.step||''))||!g.pending.map(Number).includes(pid))break;g.completed=[...new Set([...(g.completed||[]).map(Number),pid])];g.pending=g.pending.map(Number).filter(x=>x!==pid&&!state.fallen?.[x]);g.complete=g.pending.length===0;appendFeed(state,'Player '+pid+' completed '+String(g.kind||'combat')+(g.step?' '+String(g.step)+' step':'')+' turn-based actions.'+(g.complete?' Combat priority can begin.':''));break;}
      case 'ATTACKER_DECLARED':{const oid=String(p.objectId||''),o=state.battle.find(x=>String(x.id)===oid&&!(typeof MTGPhasing!=='undefined'&&MTGPhasing.isPhasedOut(x))),d=p.defender||{},actor=Number(id),ctrl=Number(o?.controllerId??o?.owner??0);if(!o||!oid)break;if(String(state.phase||'').toUpperCase()!=='DECLARE ATTACKERS'||actor!==Number(state.active)||ctrl!==Number(state.active)){appendFeed(state,(o.name||'Permanent')+' could not be declared as an attacker outside its controller’s declare attackers action.');break}if(!/\bCreature\b/i.test(String(o.typeLine||o.type||''))||o.tapped){appendFeed(state,(o.name||'Permanent')+' is not an untapped creature that can attack.');break}let atr=typeof MTGCombatRules!=='undefined'?MTGCombatRules.stats(o,state).traits:(o.combatMeta?.traits||{});if(atr.defender){appendFeed(state,(o.name||'Creature')+' has defender and cannot attack.');break}let controlledTurn=Number(o.controlledSinceTurn??o.soloNpcBornTurn??0);if(controlledTurn>=Number(state.turn||1)&&!atr.haste){appendFeed(state,(o.name||'Creature')+' cannot attack this turn without haste.');break}let defender=null;if(String(d.kind)==='player'&&Number(d.playerId)&&Number(d.playerId)!==ctrl&&!state.fallen?.[Number(d.playerId)])defender={kind:'player',playerId:Number(d.playerId)};else if(String(d.kind)==='object'&&String(d.objectId||'')&&String(d.objectId)!==oid){let target=state.battle.find(x=>String(x.id)===String(d.objectId)&&!(typeof MTGPhasing!=='undefined'&&MTGPhasing.isPhasedOut(x)));if(target&&/\bPlaneswalker\b/i.test(String(target.typeLine||target.type||''))&&Number(target.controllerId??target.owner)!==ctrl)defender={kind:'object',objectId:String(d.objectId),defendingPlayerId:Number(target.controllerId??target.owner)}}if(!defender){appendFeed(state,(o.name||'Creature')+' did not have a legal tracked defender.');break}state.combat.active=true;state.combat.hadAttackersThisCombat=true;state.combat.attackers=state.combat.attackers.filter(a=>String(a.objectId)!==oid);state.combat.attackers.push({objectId:oid,defender,blocked:false});let vig=typeof MTGContinuousEffects!=='undefined'?!!MTGContinuousEffects.derived(state,o).traits.vigilance:!!o.combatMeta?.traits?.vigilance;if(!vig)o.tapped=true;appendFeed(state,(o.name||'Permanent')+' declared as an attacker.'+(vig?'':' It tapped to attack.'));break;}
      case 'ATTACKER_REMOVED':{const oid=String(p.objectId||'');state.combat.attackers=state.combat.attackers.filter(a=>String(a.objectId)!==oid);state.combat.damageAssignments=state.combat.damageAssignments.filter(a=>String(a.sourceObjectId)!==oid&&!(a.target?.kind==='object'&&String(a.target.objectId)===oid));if(!state.combat.damageAssignments.length)state.combat.damageResolved=false;if(!state.combat.attackers.length&&!state.combat.blockers.length)state.combat.active=false;break;}
      case 'BLOCKER_DECLARED':{const bid=String(p.blockerObjectId||''),aid=String(p.attackerObjectId||''),actor=Number(id);if(!bid||!aid||bid===aid||String(state.phase||'').toUpperCase()!=='DECLARE BLOCKERS')break;const atkRec=state.combat.attackers.find(a=>String(a.objectId)===aid),atk=state.battle.find(x=>String(x.id)===aid),blk=state.battle.find(x=>String(x.id)===bid&&!(typeof MTGPhasing!=='undefined'&&MTGPhasing.isPhasedOut(x)));if(!atkRec||!atk||!blk||Number(blk.controllerId??blk.owner)!==actor)break;let defendingPlayer=atkRec.defender?.kind==='player'?Number(atkRec.defender.playerId):atkRec.defender?.kind==='object'?Number(atkRec.defender.defendingPlayerId??state.battle.find(x=>String(x.id)===String(atkRec.defender.objectId))?.controllerId??state.battle.find(x=>String(x.id)===String(atkRec.defender.objectId))?.owner??0):0;if(defendingPlayer!==actor){appendFeed(state,(blk.name||'Creature')+' cannot block an attacker that is not attacking that player or a tracked object they control.');break}let blockCheck=typeof MTGCombatKeywords!=='undefined'?MTGCombatKeywords.canBlock(atk,blk,state):(!/\bCreature\b/i.test(String(blk.typeLine||blk.type||''))||blk.tapped?{ok:false,reason:'illegal-blocker'}:{ok:true});if(!blockCheck.ok){appendFeed(state,(blk?.name||'Permanent')+' cannot block '+(atk?.name||'that attacker')+' · '+String(blockCheck.reason||'illegal block')+'.');break;}state.combat.active=true;state.combat.blockers=state.combat.blockers.filter(b=>String(b.blockerObjectId)!==bid);state.combat.blockers.push({blockerObjectId:bid,attackerObjectId:aid});let ar=state.combat.attackers.find(a=>String(a.objectId)===aid);if(ar)ar.blocked=true;appendFeed(state,(blk?.name||'Permanent')+' declared as a blocker.');break;}
      case 'BLOCKER_REMOVED':{const bid=String(p.blockerObjectId||'');state.combat.blockers=state.combat.blockers.filter(b=>String(b.blockerObjectId)!==bid);state.combat.damageAssignments=state.combat.damageAssignments.filter(a=>String(a.sourceObjectId)!==bid&&!(a.target?.kind==='object'&&String(a.target.objectId)===bid));if(!state.combat.damageAssignments.length)state.combat.damageResolved=false;if(!state.combat.attackers.length&&!state.combat.blockers.length)state.combat.active=false;break;}
      case 'COMBAT_OBJECT_COMMANDER_SET':{const oid=String(p.objectId||''),o=state.battle.find(x=>String(x.id)===oid);if(!o)break;if(p.enabled===false){delete o.isCommander;delete o.commanderId;delete o.commanderOwnerId;delete o.commanderName;appendFeed(state,(o.name||'Permanent')+' is no longer tracked as a commander.');break;}const owner=Number(p.ownerId??o.owner);if(!owner)break;o.isCommander=true;o.commanderOwnerId=owner;o.commanderId=String(p.commanderId||o.commanderId||commanderKey(owner));o.commanderName=String(p.name||o.commanderName||o.name||'Commander');registerCommander(state,owner,o.commanderName,o.commanderId);appendFeed(state,(o.name||'Permanent')+' is tracked as a commander.');break;}
      case 'COMBAT_OBJECT_STATS_SET':{const oid=String(p.objectId||''),o=state.battle.find(x=>String(x.id)===oid);if(!o)break;o.combatMeta??={traits:{}};o.combatMeta.traits??={};const norm=v=>{if(v===null||v===undefined||String(v).trim()==='')return null;try{return BigInt(String(v).replace(/,/g,'').trim()).toString()}catch{return null}};o.combatMeta.power=norm(p.power);o.combatMeta.toughness=norm(p.toughness);appendFeed(state,(o.name||'Permanent')+' combat stats updated.');break;}
      case 'COMBAT_OBJECT_TRAIT_SET':{const oid=String(p.objectId||''),o=state.battle.find(x=>String(x.id)===oid),trait=String(p.trait||'');const allowed=new Set(['deathtouch','trample','vigilance','firstStrike','doubleStrike','flying','reach','menace','lifelink','indestructible','haste','defender']);if(!o||!allowed.has(trait))break;o.combatMeta??={};o.combatMeta.traits??={};o.combatMeta.traits[trait]=!!p.enabled;appendFeed(state,(o.name||'Permanent')+' '+trait+' '+(p.enabled?'enabled':'disabled')+'.');break;}
      case 'COMBAT_DAMAGE_ASSIGNED':{const sid=String(p.sourceObjectId||''),source=state.battle.find(o=>String(o.id)===sid&&!(typeof MTGPhasing!=='undefined'&&MTGPhasing.isPhasedOut(o))),t=p.target||{};if(!sid||!source)break;const step=String(p.flags?.step||'');if(state.combatSteps?.active){const cur=String(state.combatSteps.current||'');if(step!==cur)break;const eligible=typeof MTGAdvancedCombatDamage!=='undefined'?MTGAdvancedCombatDamage.stepEligible(state,source,step):(step==='first'?!!(source.combatMeta?.traits?.firstStrike||source.combatMeta?.traits?.doubleStrike):!!(source.combatMeta?.traits?.doubleStrike||!source.combatMeta?.traits?.firstStrike));if(!eligible)break;}let target=null;if(String(t.kind)==='player'&&Number(t.playerId))target={kind:'player',playerId:Number(t.playerId)};else if(String(t.kind)==='object'&&String(t.objectId||'')&&state.battle.some(o=>String(o.id)===String(t.objectId)&&!(typeof MTGPhasing!=='undefined'&&MTGPhasing.isPhasedOut(o))))target={kind:'object',objectId:String(t.objectId)};if(!target)break;let amount;try{amount=BigInt(String(p.amount??0).replace(/,/g,''));}catch{amount=0n}if(amount<0n)amount=0n;const assignmentId=String(p.assignmentId||('dmg_'+sid+'_'+(target.kind==='player'?'p'+target.playerId:'o'+target.objectId))),v=typeof MTGAdvancedCombatDamage!=='undefined'?MTGAdvancedCombatDamage.validateIncremental(state,sid,target,amount.toString(),step||null,assignmentId):{ok:true};if(!v.ok){appendFeed(state,(source.name||'Permanent')+' combat damage assignment rejected · '+String(v.reason||'illegal assignment')+'.');break}if(v.review)recordRulesReview(state,event,'combat-assignment-review','Combat damage assignment needs manual confirmation because source power is not publicly known.',Number(source.controllerId??source.owner??id));state.combat.damageAssignments=state.combat.damageAssignments.filter(a=>String(a.assignmentId)!==assignmentId);const tr=typeof MTGContinuousEffects!=='undefined'?MTGContinuousEffects.derived(state,source).traits:(source.combatMeta?.traits||{}),flags={};if(tr.deathtouch)flags.deathtouch=true;if(tr.trample)flags.trample=true;if(tr.lifelink)flags.lifelink=true;if(p.flags?.cantBePrevented)flags.cantBePrevented=true;if(step==='first'||step==='normal')flags.step=step;if(amount>0n)state.combat.damageAssignments.push({assignmentId,sourceObjectId:sid,target,amount:amount.toString(),...(Object.keys(flags).length?{flags}:{})});state.combat.damageResolved=false;appendFeed(state,(source.name||'Permanent')+' assigned '+amount.toString()+' combat damage.');break;}
      case 'COMBAT_DAMAGE_ASSIGNMENT_REMOVED':{const aid=String(p.assignmentId||'');state.combat.damageAssignments=state.combat.damageAssignments.filter(a=>String(a.assignmentId)!==aid);state.combat.damageResolved=false;break;}
      case 'COMBAT_DAMAGE_SEQUENCE_STARTED':{const first=typeof MTGCombatSteps!=='undefined'?MTGCombatSteps.needsFirstStep(state):!!p.firstStepRequired,snap=typeof MTGAdvancedCombatDamage!=='undefined'?MTGAdvancedCombatDamage.firstSnapshot(state):{captured:false,sources:{}};state.combatSteps={active:true,current:first?'first':'normal',firstStepRequired:first,completed:[],resolvedAssignmentIds:[],sequence:Number(state.combatSteps?.sequence||0)+1,firstSnapshot:snap};state.combat.damageAssignments=[];state.combat.damageResolved=false;state.combat.actionGate=null;appendFeed(state,'Combat damage sequence started at '+(first?'first-strike':'normal')+' step.');break;}
      case 'COMBAT_DAMAGE_STEP_RESOLVED':{const step=String(p.step||state.combatSteps?.current||'');if(!state.combatSteps?.active||!['first','normal'].includes(step)||String(state.combatSteps.current)!==step)break;const done=new Set((state.combatSteps.resolvedAssignmentIds||[]).map(String)),pending=state.combat.damageAssignments.filter(a=>String(a.flags?.step||'')===step&&!done.has(String(a.assignmentId))),r=phase94ResolveCombatBatch(state,event,pending,{step,strict:true});if(!r.ok)break;for(const x of r.resolvedIds)done.add(String(x));state.combatSteps.resolvedAssignmentIds=[...done];if(!state.combatSteps.completed.includes(step))state.combatSteps.completed.push(step);state.combat.damageResolved=state.combat.damageAssignments.filter(a=>String(a.flags?.step||'')===step).every(a=>done.has(String(a.assignmentId)));appendFeed(state,(step==='first'?'First-strike':'Normal')+' combat damage step resolved simultaneously.');break;}
      case 'COMBAT_DAMAGE_STEP_ADVANCED':{if(!state.combatSteps?.active)break;const cur=String(state.combatSteps.current||'');if(cur==='first'){state.combatSteps.current='normal';state.combat.damageAssignments=[];state.combat.damageResolved=false;state.combat.actionGate=null;appendFeed(state,'Combat damage advanced to normal step.');}else if(cur==='normal'){state.combatSteps.current='complete';state.combatSteps.active=false;state.combat.damageAssignments=[];state.combat.damageResolved=false;state.combat.actionGate=null;appendFeed(state,'Combat damage sequence complete.');}break;}
      case 'COMBAT_DAMAGE_EFFECT_ADDED':{const kind=String(p.kind||'prevent');if(!['prevent','redirect'].includes(kind))break;const effectId=String(p.effectId||uid('damagefx'));const e={effectId,kind,scope:p.scope&&typeof p.scope==='object'?clone(p.scope):{},oneShot:p.oneShot!==false,used:false,order:Number(p.order??state.combat.damageEffects.length)};if(kind==='prevent'){const amt=asBigInt(p.remainingAmount??p.amount);if(amt<=0n)break;e.remainingAmount=amt.toString();}else{if(!validDamageTarget(state,p.toTarget))break;e.toTarget=clone(p.toTarget);}state.combat.damageEffects=state.combat.damageEffects.filter(x=>String(x.effectId)!==effectId);state.combat.damageEffects.push(e);appendFeed(state,'Combat damage effect added.');break;}
      case 'COMBAT_DAMAGE_EFFECT_REMOVED':{const eid=String(p.effectId||'');state.combat.damageEffects=state.combat.damageEffects.filter(x=>String(x.effectId)!==eid);appendFeed(state,'Combat damage effect removed.');break;}
      case 'COMBAT_DAMAGE_EFFECTS_CLEARED':{state.combat.damageEffects=[];appendFeed(state,'Combat damage effects cleared.');break;}
      case 'COMBAT_DAMAGE_RESOLVED':{if(state.combat.damageResolved)break;let r=phase94ResolveCombatBatch(state,event,state.combat.damageAssignments,{step:null,strict:true});if(!r.ok)break;state.combat.damageResolved=true;appendFeed(state,'Combat damage resolved simultaneously.');break;}
      case 'COMBAT_DAMAGE_CLEARED':{state.combat.damageAssignments=[];state.combat.damageResolved=false;appendFeed(state,'Combat damage assignments cleared.');break;}
      case 'COMBAT_DAMAGE_MARKS_CLEARED':{for(const o of state.battle){o.damageMarked='0';o.deathtouchMarked=false;}appendFeed(state,'Marked combat damage cleared.');break;}
      case 'END_STEP_DAMAGE_CLEANUP':{for(const o of state.battle){o.damageMarked='0';o.deathtouchMarked=false;}appendFeed(state,'End-step marked damage cleared.');break;}
      case 'STATE_BASED_ACTIONS_APPLIED':{const ids=new Set((Array.isArray(p.objectIds)?p.objectIds:[]).map(String));for(const rid of [...ids]){let i=state.battle.findIndex(o=>String(o.id)===rid&&!(typeof MTGPhasing!=='undefined'&&MTGPhasing.isPhasedOut(o)));if(i<0)continue;let o=state.battle[i],t=null,d=0n,derived=typeof MTGContinuousEffects!=='undefined'?MTGContinuousEffects.derived(state,o):null;try{let tv=derived?derived.toughness:o.combatMeta?.toughness;if(tv!==null&&tv!==undefined&&String(tv).trim()!=='')t=BigInt(String(tv))}catch{}try{d=BigInt(String(o.damageMarked||0))}catch{}if(t===null)continue;const zero=t<=0n,lethal=zero||d>=t||(!!o.deathtouchMarked&&d>0n),ind=derived?!!derived.traits.indestructible:!!o.combatMeta?.traits?.indestructible;if(!lethal||(!zero&&ind))continue;o=state.battle.splice(i,1)[0];if(typeof MTGLKI!=='undefined')MTGLKI.nextIncarnation(o,event,'graveyard');if(typeof MTGCardForms!=='undefined'){let fr=MTGCardForms.onZoneChange(o,'graveyard',{},event);if(fr.review)recordRulesReview(state,event,'card-form-zone-reveal','A face-down object changed zones and its private identity must be revealed by its owner.',Number(o.owner||id));}o.tapped=false;o.controllerId=Number(o.owner)||o.controllerId;o.c={};state.zones.graveyard??=[];state.zones.graveyard.push(o);recordCommanderZoneChoice(state,o,'graveyard',event);unlinkObjectAttachments(state,rid,event,'state-based-object-left');for(const set of Object.values(state.targeting?.sets||{}))set.targets=(set.targets||[]).filter(x=>!(x.kind==='object'&&String(x.objectId)===rid));state.combat.attackers=state.combat.attackers.filter(a=>String(a.objectId)!==rid);state.combat.blockers=state.combat.blockers.filter(b=>String(b.blockerObjectId)!==rid);state.combat.damageAssignments=state.combat.damageAssignments.filter(a=>String(a.sourceObjectId)!==rid&&!(a.target?.kind==='object'&&String(a.target.objectId)===rid));appendFeed(state,(o.name||'Permanent')+' moved to graveyard by state-based actions.');}if(typeof MTGProtectionRules!=='undefined'){let pa=MTGProtectionRules.applyAttachmentSBAs(state);event.phase90AttachmentActions=clone(pa.applied||[]);for(const a of pa.applied||[])if(a.kind==='aura-graveyard'){let moved=(state.zones.graveyard||[]).find(o=>String(o.id)===String(a.sourceObjectId));if(moved){if(typeof MTGLKI!=='undefined')MTGLKI.nextIncarnation(moved,event,'graveyard');recordCommanderZoneChoice(state,moved,'graveyard',event)}}for(const a of pa.applied||[])appendFeed(state,a.kind==='aura-graveyard'?'Protection put an illegally attached Aura into its owner’s graveyard.':'Protection caused an Equipment/Fortification to become unattached.');if(pa.review?.length)recordRulesReview(state,event,'protection-attachment-review','An attached object has a protection interaction requiring manual review.',id);}if(!state.combat.damageAssignments.length)state.combat.damageResolved=false;if(!state.combat.attackers.length&&!state.combat.blockers.length)state.combat.active=false;break;}
      case 'COMBAT_CLEARED':{state.combat={active:false,hadAttackersThisCombat:false,attackers:[],blockers:[],damageAssignments:[],damageResolved:false,damageEffects:state.combat?.damageEffects||[]};state.combatSteps={active:false,current:'idle',firstStepRequired:false,completed:[],resolvedAssignmentIds:[],sequence:Number(state.combatSteps?.sequence||0),firstSnapshot:{captured:false,sources:{}}};appendFeed(state,'Combat assignments cleared.');break;}
      case 'PERMANENT_ADDED':{if(p.object){let o=clone(p.object);if(o.phase100FromStack&&typeof MTGLKI!=='undefined'){MTGLKI.nextIncarnation(o,event,'battlefield');delete o.phase100FromStack;}if(typeof MTGReplacementOrdering!=='undefined'&&!p._phase87ReplacementResolved){let ctx=MTGReplacementOrdering.contextFor('PERMANENT_ADDED',{object:o}),plan=ctx?MTGReplacementOrdering.pending(state,'PERMANENT_ADDED',{object:o},ctx,Number(ctx.affectedPlayerId||id),[],o):null;if(plan?.review?.length)recordRulesReview(state,event,'enter-replacement-review','An enter-the-battlefield replacement could not be safely ordered automatically.',ctx?.affectedPlayerId);if(plan?.pending){plan.pending.actorId=id;plan.pending.source=event.source;state.replacementOrderPending=plan.pending;event.replacementDeferred=true;appendFeed(state,'REPLACEMENT ORDER · Player '+plan.pending.playerId+' must order how this permanent enters.');break}if(plan){o=clone(plan.ctx.object);Object.assign(p,MTGReplacementOrdering.payloadFrom('PERMANENT_ADDED',{object:o},plan.ctx));event.phase87ReplacementApplied=(plan.applied||[]).slice();}}normalizePermanent(o,state.battle.length);markControlledSinceTurn(o,state);if(typeof MTGLKI!=='undefined'&&!o.zoneEnteredAtSeq){o.zoneEnteredAtSeq=Number(event.seq||0);o.zone='battlefield';}state.battle.push(o);if(o.isCommander||o.commanderId){let ci=commanderIdentityForObject(o);if(ci)registerCommander(state,ci.ownerId,ci.name,ci.commanderId)}if(typeof MTGAlternateZoneCasting!=='undefined')event.phase103PermanentResult=clone(MTGAlternateZoneCasting.onPermanentAdded(state,o,event));if(typeof MTGAlternativeCastingModes!=='undefined')event.phase105PermanentResult=clone(MTGAlternativeCastingModes.onPermanentAdded(state,o,event));if(typeof MTGOptionalCastingCosts!=='undefined')event.phase106PermanentResult=clone(MTGOptionalCastingCosts.onPermanentAdded(state,o,event));p.object=clone(o);}break;}
      case 'PERMANENT_TAPPED_SET':{const o=state.battle.find(x=>x.id===p.objectId&&!(typeof MTGPhasing!=='undefined'&&MTGPhasing.isPhasedOut(x)));if(!o)break;o.tapped=!!p.tapped;appendFeed(state,(o.name||'Permanent')+' was '+(o.tapped?'tapped':'untapped')+'.');break;}
      case 'PERMANENT_PHASE_STATUS_SET':{if(typeof MTGPhasing==='undefined')break;const oid=String(p.objectId||''),want=String(p.status||'').toLowerCase(),r=want==='out'||want==='phased_out'?MTGPhasing.phaseOut(state,oid,event,{reason:p.reason||'manual',returnPlayerId:p.returnPlayerId}):want==='in'||want==='phased_in'?MTGPhasing.phaseIn(state,oid,event,{reason:p.reason||'manual'}):{ok:false,reason:'invalid-status',changed:[]};event.phase96Result=clone(r);if(r.ok)appendFeed(state,(state.battle.find(o=>String(o.id)===oid)?.name||'Permanent')+' '+(r.status===MTGPhasing.PHASED_OUT?'phased out.':'phased in.'));break;}
      case 'PERMANENT_FACE_SET':{if(typeof MTGCardForms==='undefined')break;let o=state.battle.find(x=>String(x.id)===String(p.objectId||''));if(!o)break;let r=MTGCardForms.applyFace(o,p.faceId,event,{orientation:p.orientation||p.faceId});event.phase98Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'card-face-set','The requested card face could not be proven from public form data.',Number(o.controllerId??o.owner??id));if(r.ok&&r.changed)appendFeed(state,(o.name||'Permanent')+' changed to another public face.');break;}
      case 'PERMANENT_TRANSFORMED':{if(typeof MTGCardForms==='undefined')break;let o=state.battle.find(x=>String(x.id)===String(p.objectId||''));if(!o)break;let r=MTGCardForms.transform(o,event);event.phase98Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'card-transform','Transform could not be resolved from the tracked public faces.',Number(o.controllerId??o.owner??id));if(r.ok&&r.changed)appendFeed(state,(o.name||'Permanent')+' transformed.');break;}
      case 'PERMANENT_FACE_DOWN_SET':{if(typeof MTGCardForms==='undefined')break;let o=state.battle.find(x=>String(x.id)===String(p.objectId||''));if(!o)break;let r=MTGCardForms.setFaceDown(o,p.publicCharacteristics||{},event,{privateIdentityRef:p.privateIdentityRef});event.phase98Result=clone(r);if(r.ok)appendFeed(state,'A permanent was turned face down using tracked public characteristics.');break;}
      case 'PERMANENT_FACE_UP_SET':{if(typeof MTGCardForms==='undefined')break;let o=state.battle.find(x=>String(x.id)===String(p.objectId||''));if(!o)break;let r=MTGCardForms.turnFaceUp(o,p.faceId||null,event);event.phase98Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'card-face-up','Turning this object face up requires private identity data the shared engine does not have.',Number(o.controllerId??o.owner??id));if(r.ok)appendFeed(state,(o.name||'Permanent')+' was turned face up.');break;}
      case 'FACE_DOWN_PERMANENT_ENTERED':{if(typeof MTGFaceDownMechanics==='undefined')break;let r=MTGFaceDownMechanics.makePermanent({objectId:p.objectId,ownerId:p.ownerId??id,controllerId:p.controllerId??id,mechanism:p.mechanism,origin:p.origin||p.mechanism,privateIdentityRef:p.privateIdentityRef,publicSequence:p.publicSequence},event);event.phase99Result=clone(r);if(!r.ok){if(r.review)recordRulesReview(state,event,'face-down-entry','A face-down permanent could not be represented safely.',Number(p.controllerId??id));break}let o=r.object;normalizePermanent(o,state.battle.length);markControlledSinceTurn(o,state);if(typeof MTGLKI!=='undefined'&&!o.zoneEnteredAtSeq){o.zoneEnteredAtSeq=Number(event.seq||0);o.zone='battlefield';}state.battle.push(o);p.objectId=String(o.id);appendFeed(state,'A '+String(r.mechanism||'face-down')+' permanent entered the battlefield face down.');break;}
      case 'PERMANENT_FACE_UP_SPECIAL_ACTION':{if(typeof MTGFaceDownMechanics==='undefined')break;let o=state.battle.find(x=>String(x.id)===String(p.objectId||''));if(!o)break;if(Number(o.controllerId??o.owner)!==Number(id)){event.phase99Result={ok:false,reason:'only-controller-may-turn-face-up',review:false};break}let r=MTGFaceDownMechanics.turnFaceUpSpecial(state,o,p.reveal||{},event,{route:p.route,paymentConfirmed:p.paymentConfirmed===true,xValue:p.xValue});event.phase99Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'face-down-turn-up','The requested turn-face-up special action could not be proven from the revealed card.',Number(id));if(r.ok)appendFeed(state,(o.name||'Permanent')+' was turned face up as a special action'+(r.megamorphCounter?' and received a +1/+1 counter.':'.'));break;}
      case 'PREPARED_EXILE_FORETOLD':{if(typeof MTGPreparedExile==='undefined')break;let r=MTGPreparedExile.foretell(state,{...p,playerId:Number(p.playerId??id)},event);event.phase101PreparedResult=clone(r);if(!r.ok){if(r.review)recordRulesReview(state,event,'foretell-special-action','Foretell could not be represented safely: '+String(r.reason||'unknown')+'.',Number(p.playerId??id));break}appendFeed(state,'Player '+Number(p.playerId??id)+' foretold a card.');break;}
      case 'PREPARED_EXILE_PLOTTED':{if(typeof MTGPreparedExile==='undefined')break;let r=MTGPreparedExile.plot(state,{...p,playerId:Number(p.playerId??id)},event);event.phase101PreparedResult=clone(r);if(!r.ok){if(r.review)recordRulesReview(state,event,'plot-special-action','Plot could not be represented safely: '+String(r.reason||'unknown')+'.',Number(p.playerId??id));break}appendFeed(state,(r.object?.name||'A card')+' was plotted by Player '+Number(p.playerId??id)+'.');break;}
      case 'PREPARED_EXILE_MARK_PLOTTED':{if(typeof MTGPreparedExile==='undefined')break;let r=MTGPreparedExile.markPlotted(state,p.objectId,Number(p.playerId??id),event);event.phase101PreparedResult=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'became-plotted','An exile object could not be marked plotted exactly.',Number(p.playerId??id));else if(r.ok)appendFeed(state,(r.object?.name||'A card')+' became plotted.');break;}
      case 'SUSPEND_CARD_EXILED':{if(typeof MTGSuspend==='undefined')break;let r=MTGSuspend.suspendCard(state,{...p,playerId:Number(p.playerId??id)},event);event.phase102SuspendResult=clone(r);if(!r.ok){if(r.review)recordRulesReview(state,event,'suspend-special-action','Suspend could not be represented safely: '+String(r.reason||'unknown')+'.',Number(p.playerId??id));break}appendFeed(state,(r.object?.name||'A card')+' was suspended with '+r.record.timeRemaining+' time counter'+(r.record.timeRemaining===1?'':'s')+'.');break;}
      case 'SUSPEND_TIME_COUNTER_REMOVED':{if(typeof MTGSuspend==='undefined')break;let r=MTGSuspend.removeTimeCounter(state,p,event);event.phase102SuspendResult=clone(r);if(r.ok)appendFeed(state,(r.object?.name||'Suspended card')+' had a time counter removed'+(r.lastRemoved?' · last time counter removed.':'.'));break;}
      case 'SUSPEND_CAST_PERMISSION_GRANTED':{if(typeof MTGSuspend==='undefined')break;let r=MTGSuspend.grantCastPermission(state,p,event);event.phase102SuspendResult=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'suspend-cast-window','Suspend cast permission could not be bound to the exact exiled card.',Number(p.playerId??id));break;}
      case 'SUSPEND_CAST_DECLINED':{if(typeof MTGSuspend!=='undefined')event.phase102SuspendResult=clone(MTGSuspend.markDeclined(state,p.recordId,event));break;}case 'ALT_ZONE_FLASHBACK_PREPARED':{if(typeof MTGAlternateZoneCasting==='undefined')break;let r=MTGAlternateZoneCasting.prepareFlashback(state,{...p,playerId:Number(p.playerId??id)},event);event.phase103Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'flashback-cast-permission','Flashback could not be bound to the exact graveyard card.',Number(p.playerId??id));break;}
      case 'ALT_ZONE_ESCAPE_PREPARED':{if(typeof MTGAlternateZoneCasting==='undefined')break;let r=MTGAlternateZoneCasting.prepareEscape(state,{...p,playerId:Number(p.playerId??id)},event);event.phase103Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'escape-cast-permission','Escape could not be bound to the exact graveyard card/payment set.',Number(p.playerId??id));break;}
      case 'ALT_ZONE_DISTURB_PREPARED':{if(typeof MTGAlternateZoneCasting==='undefined')break;let r=MTGAlternateZoneCasting.prepareDisturb(state,{...p,playerId:Number(p.playerId??id)},event);event.phase103Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'disturb-cast-permission','Disturb could not bind the exact graveyard card to its back-face cast.',Number(p.playerId??id));break;}
      case 'ALT_ZONE_RETRACE_PREPARED':{if(typeof MTGAlternateZoneCasting==='undefined')break;let r=MTGAlternateZoneCasting.prepareRetrace(state,{...p,playerId:Number(p.playerId??id)},event);event.phase103Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'retrace-cast-permission','Retrace could not bind the exact graveyard card.',Number(p.playerId??id));break;}
      case 'ALT_ZONE_JUMP_START_PREPARED':{if(typeof MTGAlternateZoneCasting==='undefined')break;let r=MTGAlternateZoneCasting.prepareJumpStart(state,{...p,playerId:Number(p.playerId??id)},event);event.phase103Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'jump-start-cast-permission','Jump-start could not bind the exact graveyard card.',Number(p.playerId??id));break;}
      case 'ALT_ZONE_AFTERMATH_PREPARED':{if(typeof MTGAlternateZoneCasting==='undefined')break;let r=MTGAlternateZoneCasting.prepareAftermath(state,{...p,playerId:Number(p.playerId??id)},event);event.phase103Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'aftermath-cast-permission','Aftermath could not bind the exact graveyard card to its Aftermath half.',Number(p.playerId??id));break;}

      case 'ALT_ZONE_ESCAPE_PAYMENT_COMMITTED':{if(typeof MTGAlternateZoneCasting==='undefined')break;let r=MTGAlternateZoneCasting.markEscapePaymentCommitted(state,p.permissionId,event);event.phase103Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'escape-payment-commit','Escape payment was moved but the exact permission transaction could not be finalized.',Number(p.playerId??id));break;}
      case 'ALT_ZONE_MADNESS_DISCARDED':{if(typeof MTGAlternateZoneCasting==='undefined')break;let r=MTGAlternateZoneCasting.discardMadness(state,{...p,playerId:Number(p.playerId??id)},event);event.phase103Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'madness-discard','Madness discard could not be represented exactly.',Number(p.playerId??id));else if(r.ok)appendFeed(state,(r.object?.name||'A card')+' was discarded into exile for madness.');break;}
      case 'ALT_ZONE_MADNESS_PERMISSION_GRANTED':{if(typeof MTGAlternateZoneCasting==='undefined')break;let r=MTGAlternateZoneCasting.grantMadnessPermission(state,p,event);event.phase103Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'madness-cast-window','Madness cast permission could not be bound exactly.',Number(p.playerId??id));break;}
      case 'ALT_ZONE_MADNESS_DECLINED':{if(typeof MTGAlternateZoneCasting!=='undefined')event.phase103Result=clone(MTGAlternateZoneCasting.declineMadness(state,p.recordId,event));break;}
      case 'ALT_ZONE_REBOUND_TRIGGERED':{if(typeof MTGAlternateZoneCasting!=='undefined')event.phase103Result=clone(MTGAlternateZoneCasting.markReboundTriggered(state,p.recordId,event));break;}
      case 'ALT_ZONE_REBOUND_PERMISSION_GRANTED':{if(typeof MTGAlternateZoneCasting==='undefined')break;let r=MTGAlternateZoneCasting.grantReboundPermission(state,p,event);event.phase103Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'rebound-cast-window','Rebound cast permission could not be bound to the exact exiled card.',Number(p.playerId??id));break;}
      case 'ALT_ZONE_REBOUND_DECLINED':{if(typeof MTGAlternateZoneCasting!=='undefined')event.phase103Result=clone(MTGAlternateZoneCasting.declineRebound(state,p.recordId,event));break;}
      case 'ALT_MODE_TRIGGER_QUEUED':{if(typeof MTGAlternativeCastingModes!=='undefined')event.phase105Result=clone(MTGAlternativeCastingModes.markTriggerQueued(state,p,event));break;}
      case 'OPTIONAL_CAST_TRIGGER_QUEUED':{if(typeof MTGOptionalCastingCosts!=='undefined')event.phase106Result=clone(MTGOptionalCastingCosts.markTriggerQueued(state,p,event));break;}

      case 'ALT_ZONE_WARP_TRIGGERED':{if(typeof MTGAlternateZoneCasting!=='undefined')event.phase103Result=clone(MTGAlternateZoneCasting.markWarpTriggered(state,p.recordId,event));break;}
      case 'ALT_ZONE_WARP_PERMISSION_GRANTED':{if(typeof MTGAlternateZoneCasting==='undefined')break;let r=MTGAlternateZoneCasting.grantWarpFuturePermission(state,p,event);event.phase103Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'warp-future-cast','Warp future cast permission could not be bound exactly.',Number(p.playerId??id));break;}
      case 'CAST_PERMISSION_GRANTED':{if(typeof MTGCastPermissions==='undefined')break;let r=MTGCastPermissions.grant(state,p.objectId,p.permission||{},event);event.phase100PermissionResult=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'cast-permission-grant','A cast-from-zone permission could not be bound to an exact object incarnation.',Number(p.playerId??id));break;}
      case 'CAST_PERMISSION_REVOKED':{if(typeof MTGCastPermissions!=='undefined')event.phase100PermissionResult=clone(MTGCastPermissions.revoke(state,p.permissionId,event,p.reason||'revoked'));break;}
      case 'ADVENTURE_RESOLVED_TO_EXILE':{if(typeof MTGCastPermissions==='undefined')break;let r=MTGCastPermissions.resolveAdventure(state,p.stackItem||{},event);event.phase100AdventureResult=clone(r);if(!r.ok)recordRulesReview(state,event,'adventure-resolution','Adventure resolved but the exact exile permission could not be established.',Number(p.stackItem?.controllerId??id));else appendFeed(state,(r.object?.name||'Adventure card')+' finished its Adventure and was exiled with permission to cast its permanent face.');break;}
      case 'TRACKED_CAST_NONPERMANENT_RESOLVED':{if(typeof MTGCastPermissions==='undefined')break;let r=MTGCastPermissions.finishTrackedNonPermanent(state,p.stackItem||{},event);event.phase100TrackedResolve=clone(r);if(!r.ok&&!r.ignored)recordRulesReview(state,event,'tracked-spell-resolution','A tracked spell resolved but its physical card destination could not be established.',Number(p.stackItem?.controllerId??id));break;}
      case 'PHASE106_BUYBACK_RESOLVED':{appendFeed(state,String(p.name||'Spell')+' resolved with Buyback paid · return the physical card to its owner’s hand.');break;}
      case 'PERMANENT_CONTROLLER_CHANGED':{const o=state.battle.find(x=>x.id===p.objectId),to=Number(p.controllerId);if(!o||!to)break;let prior=Number(o.controllerId??o.owner),oid=String(o.id);o.controllerId=to;if(prior!==to){o.controlledSinceTurn=Number(state.turn||1);state.combat.attackers=state.combat.attackers.filter(a=>String(a.objectId)!==oid);state.combat.blockers=state.combat.blockers.filter(b=>String(b.blockerObjectId)!==oid);state.combat.damageAssignments=state.combat.damageAssignments.filter(a=>String(a.sourceObjectId)!==oid&&!(a.target?.kind==='object'&&String(a.target.objectId)===oid));if(!state.combat.damageAssignments.length)state.combat.damageResolved=false;if(!state.combat.attackers.length&&!state.combat.blockers.length)state.combat.active=false}if(o.phase102SuspendCast?.grantedHaste&&prior!==to)o.phase102SuspendCast={...o.phase102SuspendCast,grantedHaste:false,endedByControlChange:true};appendFeed(state,(o.name||'Permanent')+' is now controlled by Player '+to+'.');break;}
      case 'TOKEN_COHORT_CREATED':{
        const cohort=clone(p.cohort||{});cohort.token=1;cohort.q=String(cohort.q??p.quantity??'1');let owner=Number(cohort.controllerId??cohort.owner??id),repl=null;if(typeof MTGReplacementEffects!=='undefined'){repl=MTGReplacementEffects.applyToken(state,{affectedPlayerId:owner,quantity:cohort.q});if(repl.reviewRequired)recordRulesReview(state,event,'token-replacement','Token replacement effect requires manual review; base quantity was kept.',owner);cohort.q=repl.value;}normalizePermanent(cohort,state.battle.length);markControlledSinceTurn(cohort,state);if(typeof MTGLKI!=='undefined'&&!cohort.zoneEnteredAtSeq){cohort.zoneEnteredAtSeq=Number(event.seq||0);cohort.zone='battlefield';}state.battle.push(cohort);p.cohort=clone(cohort);
        owner=cohort.owner??id;const ps=ensurePlayerStats(state,owner);ps.tokensCreated=(asBigInt(ps.tokensCreated)+asBigInt(cohort.q)).toString();appendFeed(state,'Player '+owner+' created '+formatExact(cohort.q)+' '+(cohort.name||'token')+(asBigInt(cohort.q)===1n?'':'s')+'.');
        recordCalculation(state,event,'Token cohort creation',['Base quantity: '+formatExact(p.baseQuantity??p.cohort?.q??cohort.q),...(p.modifiers||[]).map(m=>m.label+': ×'+m.multiplier),...(repl?.applied||[]).map(x=>'Replacement '+x.effectId+': ×'+x.multiplier)],'Final quantity: '+formatExact(cohort.q),'tokens');break;
      }
      case 'TOKEN_COHORT_QUANTITY_CHANGED':{const o=state.battle.find(x=>x.id===p.objectId);if(!o||!o.token)break;o.q=String(exactNonNegativeAdd(o.q,p.delta));break;}
      case 'PERMANENT_REMOVED':{const rid=String(p.objectId||'');state.battle=state.battle.filter(o=>String(o.id)!==rid);delete state.commanderZoneChoices?.byObject?.[rid];unlinkObjectAttachments(state,rid,event,'permanent-removed');for(const set of Object.values(state.targeting?.sets||{}))set.targets=(set.targets||[]).filter(t=>!(t.kind==='object'&&String(t.objectId)===rid));state.combat.attackers=state.combat.attackers.filter(a=>String(a.objectId)!==rid);state.combat.blockers=state.combat.blockers.filter(b=>String(b.blockerObjectId)!==rid);state.combat.damageAssignments=state.combat.damageAssignments.filter(a=>String(a.sourceObjectId)!==rid&&!(a.target?.kind==='object'&&String(a.target.objectId)===rid));state.combat.damageEffects=(state.combat.damageEffects||[]).filter(e=>String(e?.scope?.sourceObjectId??e?.sourceObjectId??'')!==rid&&!(e?.scope?.target?.kind==='object'&&String(e.scope.target.objectId)===rid)&&!(e?.target?.kind==='object'&&String(e.target.objectId)===rid)&&!(e?.toTarget?.kind==='object'&&String(e.toTarget.objectId)===rid));if(!state.combat.damageAssignments.length)state.combat.damageResolved=false;if(!state.combat.attackers.length&&!state.combat.blockers.length)state.combat.active=false;break;}
      case 'OBJECT_ZONE_CHANGED':{
        const id=String(p.objectId||''),to=String(p.toZone||'battlefield').toLowerCase();if(!id)break;
        let obj=null,from='';let i=state.battle.findIndex(x=>String(x.id)===id);if(i>=0){obj=state.battle.splice(i,1)[0];from='battlefield';}
        if(!obj){for(const z of ['graveyard','exile','hand','command']){let a=state.zones[z]||[],j=a.findIndex(x=>String(x.id)===id);if(j>=0){obj=a.splice(j,1)[0];from=z;break;}}}
        if(!obj)break;
        delete state.commanderZoneChoices.byObject[id];
        if(p.reset!==false){obj.tapped=false;obj.controllerId=Number(obj.owner)||obj.controllerId;obj.c={};}
        if(p.controllerId!=null&&Number(p.controllerId))obj.controllerId=Number(p.controllerId);
        if(typeof MTGAlternativeCastingModes!=='undefined')event.phase105ZoneResult=clone(MTGAlternativeCastingModes.onZoneChange(state,obj,{...event,fromZone:from,toZone:to}));if(typeof MTGLKI!=='undefined')MTGLKI.nextIncarnation(obj,event,to);if(typeof MTGCastPermissions!=='undefined')event.phase100PermissionExpiry=clone(MTGCastPermissions.onZoneChange(state,obj,event));if(typeof MTGPreparedExile!=='undefined')event.phase101PreparedExpiry=clone(MTGPreparedExile.onZoneChange(state,obj,event));if(typeof MTGSuspend!=='undefined')event.phase102SuspendExpiry=clone(MTGSuspend.onZoneChange(state,obj,event));if(typeof MTGAlternateZoneCasting!=='undefined')event.phase103Expiry=clone(MTGAlternateZoneCasting.onZoneChange(state,obj,event));if(typeof MTGPhasing!=='undefined'){obj.phaseStatus=MTGPhasing.PHASED_IN;delete obj.phaseMode;delete obj.phaseRootId;delete obj.phaseAnchorId;delete obj.phaseReturnPlayerId;delete obj.phaseReason;delete obj.phaseBatchId;}if(typeof MTGFaceDownMechanics!=='undefined'&&obj.faceDownState?.active){let zr=MTGFaceDownMechanics.revealForZoneChange(obj,p.reveal||{},event);event.phase99ZoneReveal=clone(zr);if(zr.review)recordRulesReview(state,event,'face-down-zone-reveal','A face-down object changed zones and must be revealed to all players.',Number(obj.owner||id));}if(typeof MTGCardForms!=='undefined'){let fr=MTGCardForms.onZoneChange(obj,to,p,event);event.phase98Result=clone(fr);if(fr.review)recordRulesReview(state,event,'card-form-zone-reveal','A face-down object changed zones and its private identity must be revealed by its owner.',Number(obj.owner||id));}
        if(to==='battlefield'){normalizePermanent(obj,state.battle.length);markControlledSinceTurn(obj,state);state.battle.push(obj);}else{state.zones[to]??=[];state.zones[to].push(obj);}
        if(to!=='battlefield')unlinkObjectAttachments(state,id,event,'zone-change');
        for(const set of Object.values(state.targeting?.sets||{}))set.targets=(set.targets||[]).filter(t=>!(t.kind==='object'&&String(t.objectId)===id));state.combat.damageEffects=(state.combat.damageEffects||[]).filter(e=>String(e?.scope?.sourceObjectId??e?.sourceObjectId??'')!==id&&!(e?.scope?.target?.kind==='object'&&String(e.scope.target.objectId)===id)&&!(e?.target?.kind==='object'&&String(e.target.objectId)===id)&&!(e?.toTarget?.kind==='object'&&String(e.toTarget.objectId)===id));if(to!=='battlefield'){state.combat.attackers=state.combat.attackers.filter(a=>String(a.objectId)!==id);state.combat.blockers=state.combat.blockers.filter(b=>String(b.blockerObjectId)!==id);state.combat.damageAssignments=state.combat.damageAssignments.filter(a=>String(a.sourceObjectId)!==id&&!(a.target?.kind==='object'&&String(a.target.objectId)===id));if(!state.combat.damageAssignments.length)state.combat.damageResolved=false;if(!state.combat.attackers.length&&!state.combat.blockers.length)state.combat.active=false;}
        recordCommanderZoneChoice(state,obj,to,event);
        if(to==='exile'&&p.linkGroupId&&typeof MTGLinkedAbilities!=='undefined'){let lr=MTGLinkedAbilities.linkExiled(state,p.linkGroupId,[id],event);event.phase97LinkResult=clone(lr);if(lr.review)recordRulesReview(state,event,'linked-exile-zone-change','The exile move could not be attached to the requested linked ability exactly.',id);}
        appendFeed(state,(obj.name||'Object')+' moved to '+to+'.');break;}
      case 'COMMANDER_ZONE_CHOICE_RESOLVED':{
        const oid=String(p.objectId||''),pending=state.commanderZoneChoices?.byObject?.[oid];if(!pending)break;delete state.commanderZoneChoices.byObject[oid];
        if(!p.moveToCommand){appendFeed(state,(pending.name||'Commander')+' remains in '+pending.toZone+'.');break;}
        let obj=null;for(const z of ['graveyard','exile','hand']){const a=state.zones[z]||[],j=a.findIndex(x=>String(x.id)===oid);if(j>=0){obj=a.splice(j,1)[0];break;}}if(!obj)break;state.zones.command??=[];state.zones.command.push(obj);appendFeed(state,(obj.name||pending.name||'Commander')+' moved to the command zone by commander zone choice.');break;}
      case 'PERMANENT_COUNTER_CHANGED':{const o=state.battle.find(x=>x.id===p.objectId);if(!o)break;o.c??={};const k=p.counter||'+1/+1';let delta=asBigInt(p.delta||'1'),repl=null;if(delta>0n&&typeof MTGReplacementEffects!=='undefined'){repl=MTGReplacementEffects.applyCounter(state,{affectedPlayerId:Number(o.controllerId??o.owner),targetObjectId:String(o.id),amount:delta.toString()});if(repl.reviewRequired)recordRulesReview(state,event,'counter-replacement','Permanent-counter replacement requires manual review.',Number(o.controllerId??o.owner));delta=asBigInt(repl.value);}o.c[k]=String(exactNonNegativeAdd(o.c[k]||'0',delta));break;}
      case 'LINK_GROUP_CREATED':{if(typeof MTGLinkedAbilities==='undefined')break;let r=MTGLinkedAbilities.ensureGroup(state,p,event);event.phase97Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'linked-group-create','Linked ability group could not be tied to an exact source incarnation: '+String(r.reason||'unknown')+'.',id);break;}
      case 'LINK_CHOICE_RECORDED':{if(typeof MTGLinkedAbilities==='undefined')break;let r=MTGLinkedAbilities.writeChoice(state,p.linkGroupId,p.key,p.value,event,{kind:p.kind,label:p.label,playerId:p.playerId??id});event.phase97Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'linked-choice-record','Linked choice could not be stored without guessing.',p.playerId??id);break;}
      case 'LINK_COST_RECORDED':{if(typeof MTGLinkedAbilities==='undefined')break;let r=MTGLinkedAbilities.writeCost(state,p.linkGroupId,p.key,p.value,event,{label:p.label,playerId:p.playerId??id});event.phase97Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'linked-cost-record','Linked paid-cost information could not be stored without guessing.',p.playerId??id);break;}
      case 'LINK_NOTE_RECORDED':{if(typeof MTGLinkedAbilities==='undefined')break;let r=MTGLinkedAbilities.writeNote(state,p.linkGroupId,p.key,p.value,event);event.phase97Result=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'linked-note-record','Linked noted information could not be stored without guessing.',id);break;}
      case 'LINK_EXILED_OBJECTS_RECORDED':{if(typeof MTGLinkedAbilities==='undefined')break;let r=MTGLinkedAbilities.linkExiled(state,p.linkGroupId,p.objectIds||[],event);event.phase97Result=clone(r);if(r.review)recordRulesReview(state,event,'linked-exile-record','One or more objects could not be proven to be the exact cards exiled for this linked ability.',id);break;}
      case 'LINK_GROUP_RELEASED':{if(typeof MTGLinkedAbilities==='undefined')break;event.phase97Result=clone(MTGLinkedAbilities.deactivate(state,p.linkGroupId,event,p.reason||'released'));break;}
      case 'FEED_APPENDED':appendFeed(state,String(p.text||''));break;
      case 'STACK_ITEM_ADDED':{const item=clone(p.item||{});normalizeStackItem(item,state.stack.length);if(typeof MTGLinkedAbilities!=='undefined'){let lr=MTGLinkedAbilities.bindStackItem(state,item);if(lr.review)recordRulesReview(state,event,'linked-stack-reference','A stack object referenced a linked group that could not be resolved exactly.',item.controllerId||id);}if(typeof MTGCastPermissions!=='undefined'){let cr=MTGCastPermissions.commitStackItem(state,item,event);event.phase100CastCommit=clone(cr);if(!cr.ok&&cr.review)recordRulesReview(state,event,'cast-permission-commit','The exact card/permission could not be committed from its tracked zone.',item.controllerId||id);}if(typeof MTGAlternateZoneCasting!=='undefined'&&!item.phase100CardObject){let ar=MTGAlternateZoneCasting.commitSyntheticStackItem(state,item,event);event.phase103StackCommit=clone(ar);if(!ar.ok&&ar.review)recordRulesReview(state,event,'phase103-stack-commit','Alternate-zone cast could not be tied to an exact physical card identity.',item.controllerId||id);}state.stack.push(item);break;}
      case 'STACK_TRIGGER_GROUP_ADDED':{
        const item=clone(p.item||{});item.kind='trigger-group';item.count=String(item.count??p.count??'1');normalizeStackItem(item,state.stack.length);if(typeof MTGLinkedAbilities!=='undefined'){let lr=MTGLinkedAbilities.bindStackItem(state,item);if(lr.review)recordRulesReview(state,event,'linked-trigger-reference','A triggered ability referenced a linked group that could not be resolved exactly.',item.controllerId||id);}state.stack.push(item);
        recordCalculation(state,event,'Grouped triggers',['Base triggers: '+formatExact(p.baseCount??1),...(p.modifiers||[]).map(m=>m.label+': +'+formatExact(m.extra))],'Final triggers: '+formatExact(item.count),'triggers');break;
      }
      case 'REPLACEMENT_ORDER_CHOICE_APPLIED':{let pending=state.replacementOrderPending;if(!pending||String(pending.id)!==String(p.choiceId)||Number(pending.playerId)!==Number(id))break;let r=typeof MTGReplacementOrdering!=='undefined'?MTGReplacementOrdering.choose(state,pending,p.effectId):{ok:false,reason:'ordering-engine-missing'};if(!r.ok){recordRulesReview(state,event,'replacement-order-choice','Replacement ordering could not continue safely: '+String(r.reason||'unknown')+'.',pending.playerId);break}if(r.review?.length)recordRulesReview(state,event,'replacement-order-review','A replacement effect in the chosen chain requires manual review.',pending.playerId);if(r.choiceRequired){pending.ctx=clone(r.ctx);pending.playerId=Number(r.ctx?.affectedPlayerId||pending.playerId);pending.used=clone(r.used||{});pending.applied=(r.applied||[]).slice();pending.candidates=(r.candidates||[]).map(e=>({id:e.id,label:MTGReplacementOrdering.label(e),kind:e.kind,sourceObjectId:e.sourceObjectId}));state.replacementOrderPending=pending;appendFeed(state,'REPLACEMENT ORDER · Another applicable effect must be chosen.');break}let type=String(pending.eventType),payload=MTGReplacementOrdering.payloadFrom(type,pending.payload||{},r.ctx||pending.ctx),actor=pending.actorId??id,source=pending.source||'replacement-order';state.replacementOrderPending=null;let synthetic={id:String(event.id)+'_resolved',seq:event.seq,type,actorId:actor,payload,at:event.at,clientId:event.clientId||null,source,atomicBatchId:event.atomicBatchId||null,version:3};if(typeof MTGLKI!=='undefined')MTGLKI.prepareEvent(state,synthetic,{atomicBatchId:event.atomicBatchId||null});reduce(state,synthetic);if(typeof MTGLKI!=='undefined')MTGLKI.finalizeEvent(state,synthetic);event.resolvedReplacementEvent=clone(synthetic);event.phase87ReplacementApplied=(r.applied||[]).slice();appendFeed(state,'REPLACEMENT ORDER · chosen sequence applied.');break;}
      case 'REPLACEMENT_ORDER_CLEARED':{state.replacementOrderPending=null;appendFeed(state,'Replacement-order checkpoint cleared by host/judge correction.');break;}
      case 'CASTING_COST_PENDING_SET':{state.castingCostPending=clone(p.pending||null);if(state.castingCostPending)appendFeed(state,'CASTING COST · Player '+Number(state.castingCostPending.playerId||id)+' is completing public/physical casting costs for '+String(state.castingCostPending.name||'a spell')+'.');break;}
      case 'CASTING_COST_CHOICE_APPLIED':{let pending=state.castingCostPending;if(!pending||String(pending.id)!==String(p.choiceId)||Number(pending.playerId)!==Number(id)||typeof MTGAdvancedCastingCosts==='undefined')break;let r=MTGAdvancedCastingCosts.applyChoice(state,pending,String(p.key||''),p.value);if(r.ok)state.castingCostPending=r.pending;break;}
      case 'CASTING_COST_REVIEW_SET':{let pending=state.castingCostPending;if(!pending||String(pending.id)!==String(p.choiceId)||Number(pending.playerId)!==Number(id))break;let reason=String(p.reason||'Casting cost state changed and requires manual review.');pending.reviewReasons=Array.isArray(pending.reviewReasons)?pending.reviewReasons:[];if(!pending.reviewReasons.includes(reason))pending.reviewReasons.push(reason);pending.choices=pending.choices&&typeof pending.choices==='object'?pending.choices:{additional:{}};pending.choices.manualReviewConfirmed=false;pending.choices.paymentConfirmed=false;pending.stage='review';recordRulesReview(state,event,'casting-cost-review',reason,pending.playerId);break;}
      case 'CASTING_COST_CONFIRMED':{let pending=state.castingCostPending;if(!pending||String(pending.id)!==String(p.choiceId)||Number(pending.playerId)!==Number(id))break;state.lastCastingCostByPlayer??={};state.lastCastingCostByPlayer[String(Number(id))]=clone(p.plan||{});if(pending.linkGroupId&&typeof MTGLinkedAbilities!=='undefined'){let lr=MTGLinkedAbilities.writeCost(state,pending.linkGroupId,pending.linkCostKey||'casting-cost',p.plan||{},event,{label:pending.name||'Casting cost',playerId:id});event.phase97CostResult=clone(lr);if(!lr.ok&&lr.review)recordRulesReview(state,event,'linked-casting-cost','Casting-cost information could not be attached to its linked ability exactly.',id);}appendFeed(state,'CASTING COST · '+String(pending.name||'Spell')+' payment confirmed'+(p.plan?.totalCost&&p.plan.totalCost!=='MANUAL'?' · '+String(p.plan.totalCost):' · MANUAL / JUDGE REVIEW')+'.');state.castingCostPending=null;break;}
      case 'CASTING_COST_CANCELLED':{let pending=state.castingCostPending;if(!pending||String(pending.id)!==String(p.choiceId)||Number(pending.playerId)!==Number(id))break;appendFeed(state,'CASTING COST · '+String(pending.name||'Spell')+' casting flow cancelled before the spell was cast.');state.castingCostPending=null;break;}
      case 'ADVANCED_RESOLUTION_PENDING_SET':{state.advancedResolutionPending=clone(p.pending||null);if(state.advancedResolutionPending)appendFeed(state,'ADVANCED RESOLUTION · Player '+Number(state.advancedResolutionPending.playerId||id)+' is completing '+String(state.advancedResolutionPending.kind||'a stack')+' choice.');break;}
      case 'ADVANCED_RESOLUTION_CHOICE_APPLIED':{let pending=state.advancedResolutionPending;if(!pending||String(pending.id)!==String(p.choiceId)||Number(pending.playerId)!==Number(id)||typeof MTGAdvancedSpellResolution==='undefined')break;let r=MTGAdvancedSpellResolution.applyChoice(state,pending,p.key,p.value);if(!r.ok){recordRulesReview(state,event,'phase92-resolution-choice','Advanced-resolution choice was rejected: '+String(r.reason||'invalid')+'.',pending.playerId);break}state.advancedResolutionPending=clone(r.pending);appendFeed(state,'ADVANCED RESOLUTION · '+String(pending.kind||'choice')+' advanced to '+String(r.pending.stage||'next')+'.');break;}
      case 'ADVANCED_RESOLUTION_REVIEW_SET':{let pending=state.advancedResolutionPending;if(!pending||String(pending.id)!==String(p.choiceId))break;pending.reviewReasons=[...new Set([...(pending.reviewReasons||[]),String(p.reason||'Manual / judge review required.')])];pending.stage='review';state.advancedResolutionPending=pending;recordRulesReview(state,event,'phase92-resolution-review',String(p.reason||'Manual / judge review required.'),pending.playerId);break;}
      case 'ADVANCED_RESOLUTION_DEFERRED_TRIGGERS_SET':{let pending=state.advancedResolutionPending;if(!pending||String(pending.id)!==String(p.choiceId))break;pending.deferredTriggerCandidates=[...(pending.deferredTriggerCandidates||[]),...clone(p.candidates||[])];state.advancedResolutionPending=pending;break;}
      case 'ADVANCED_RESOLUTION_CLEARED':{let pending=state.advancedResolutionPending;if(!pending||String(pending.id)!==String(p.choiceId))break;state.lastAdvancedResolutionByPlayer[String(Number(pending.playerId||id))]=clone(p.completed||pending);state.advancedResolutionPending=null;appendFeed(state,'ADVANCED RESOLUTION · '+String(pending.kind||'choice')+' completed.');break;}
      case 'ORACLE_CHOICE_PENDING_SET':{state.oracleChoicePending=clone(p.pending||null);appendFeed(state,'A public card choice is waiting for Player '+Number(p.pending?.playerId||id)+'.');break;}
      case 'ORACLE_CHOICE_APPLIED':{let pending=state.oracleChoicePending;if(!pending||String(pending.id)!==String(p.choiceId)||Number(pending.playerId)!==Number(id))break;let item=state.stack.find(x=>String(x.id)===String(p.stackItemId||pending.stackItemId));if(item){item.choiceState=item.choiceState&&typeof item.choiceState==='object'?item.choiceState:{};if(pending.stage==='setup'&&pending.spec?.kind==='targets'){item.targets=clone(p.value||[]);item.choiceState.targetsDone=true;}else if(pending.stage==='setup'&&pending.spec?.kind==='mode'){let ids=(p.value||[]).map(String),modes=(item.effect?.modes||[]).filter(m=>ids.includes(String(m.id)));item.choiceState.modeDone=true;item.choiceState.chosenModes=ids;if(modes.length===1)item.effect=clone(modes[0].effect||{type:'manual'});else{let es=modes.map(m=>clone(m.effect||{type:'manual'})),unsafe=es.some(e=>e.optional||e.targetSpec||e.type==='modal'||e.type==='manual');item.effect=unsafe?{type:'manual',oracleText:'Multiple chosen modes require manual resolution.',automation:'manual'}:{type:'sequence',effects:es,automation:'choice'}};}else if(pending.stage==='resolution'&&pending.spec?.kind==='may'){item.choiceState.resolutionDone=true;item.choiceState.may=!!p.value;}else if(pending.stage==='resolution'&&pending.spec?.kind==='cost-choice'){item.choiceState.phase88={done:true,kind:'cost-choice',playerId:Number(pending.playerId),pay:!!p.value?.pay,...(p.value?.objectId?{objectId:String(p.value.objectId)}:{})};}else if(pending.stage==='resolution'&&pending.spec?.kind==='opponent-mode'){item.choiceState.phase88={done:true,kind:'opponent-mode',playerId:Number(pending.playerId),modeId:String(p.value?.modeId||'')};}}if(pending.linkGroupId&&typeof MTGLinkedAbilities!=='undefined'){let key=String(pending.linkChoiceKey||pending.spec?.linkChoiceKey||'choice'),r=pending.linkRecordKind==='cost'?MTGLinkedAbilities.writeCost(state,pending.linkGroupId,key,p.value,event,{label:pending.linkLabel,playerId:id}):MTGLinkedAbilities.writeChoice(state,pending.linkGroupId,key,p.value,event,{kind:pending.linkRecordKind||pending.spec?.kind||'choice',label:pending.linkLabel,playerId:id});event.phase97ChoiceResult=clone(r);if(!r.ok&&r.review)recordRulesReview(state,event,'linked-oracle-choice','A player choice could not be attached to its linked ability exactly.',id);}state.oracleChoicePending=null;break;}
      case 'ORACLE_CHOICE_CLEARED':{state.oracleChoicePending=null;break;}
      case 'TRIGGER_ORDER_BATCH_SET':{state.triggerOrderPending=clone(p.batch||null);appendFeed(state,'Simultaneous triggers are waiting for player ordering.');break;}case 'TRIGGER_ORDER_PLAYER_SET':{if(state.triggerOrderPending&&String(state.triggerOrderPending.id)===String(p.batchId)){let g=(state.triggerOrderPending.groups||[]).find(x=>Number(x.controllerId)===Number(p.playerId??id));if(g)g.topFirst=Array.isArray(p.topFirst)?p.topFirst.map(String):[];}break;}case 'TRIGGER_ORDER_BATCH_CLEARED':{state.triggerOrderPending=null;break;}case 'STACK_ITEM_TARGETS_REVALIDATED':{let x=(state.stack||[]).find(z=>String(z.id)===String(p.stackItemId||''));if(x){x.targets=clone(p.targets||[]);x.resolutionLegality={version:'phase93-resolution-legality-countering-1',checked:true,partial:!!p.audit?.partial,allIllegal:false,legalCount:x.targets.length,illegalCount:Number(p.audit?.illegalCount||0),checkedAtSeq:Number(event.seq||state.meta?.seq||0)}}break;}case 'STACK_ITEM_REMOVED':{const target=p.stackItemId,removed=target?state.stack.find(x=>x.id===target):state.stack.at(-1);state.stack=target?state.stack.filter(x=>x.id!==target):state.stack.slice(0,-1);if(removed?.effect?.type==='suspend-cast'&&p.reason!=='resolved'&&typeof MTGSuspend!=='undefined')event.phase102SuspendCountered=clone(MTGSuspend.markLastTriggerCountered(state,removed.effect.recordId,event));if(removed?.effect?.type==='phase103-madness-cast'&&p.reason!=='resolved'&&typeof MTGAlternateZoneCasting!=='undefined')event.phase103MadnessCountered=clone(MTGAlternateZoneCasting.markMadnessCountered(state,removed.effect.recordId,event));if(removed?.effect?.type==='phase103-warp-exile'&&p.reason!=='resolved'&&typeof MTGAlternateZoneCasting!=='undefined')event.phase103WarpCountered=clone(MTGAlternateZoneCasting.markWarpTriggerCountered(state,removed.effect.recordId,event));if(removed?.effect?.type==='phase104-rebound-cast'&&p.reason!=='resolved'&&typeof MTGAlternateZoneCasting!=='undefined')event.phase104ReboundCountered=clone(MTGAlternateZoneCasting.markReboundTriggerCountered(state,removed.effect.recordId,event));if(removed?.faceDownState?.active&&p.reason!=='resolved'){event.phase99RevealRequired=true;recordRulesReview(state,event,'face-down-stack-exit','A face-down spell left the stack and must be revealed to all players.',Number(removed.controllerId||id));}if(removed&&typeof MTGCastPermissions!=='undefined'&&p.reason!=='resolved'){let xr=MTGCastPermissions.handleStackExit(state,removed,p.reason,event);event.phase100StackExit=clone(xr);if(!xr.ok&&!xr.ignored)recordRulesReview(state,event,'tracked-spell-stack-exit','A tracked card left the stack but its destination could not be established exactly.',Number(removed.controllerId||id));}break;}
      case 'STACK_CLEARED':{state.stack=[];break;}
      case 'SMART_PRIORITY_MODE_SET':{const mode=['relaxed','standard','full'].includes(String(p.mode||'').toLowerCase())?String(p.mode).toLowerCase():'standard';state.smartPriority={...(state.smartPriority||{}),mode};state.smartPriority.stopProfiles??={};if(state.priorityFlow)state.priorityFlow.mode=mode;break;}case 'SMART_PRIORITY_STOPS_SET':{const target=Number(p.playerId??id),s=p.stops&&typeof p.stops==='object'?p.stops:{};state.smartPriority??={mode:'standard',stopProfiles:{}};state.smartPriority.stopProfiles??={};state.smartPriority.stopProfiles[target]={main1:!!s.main1,combat:!!s.combat,main2:!!s.main2,end:!!s.end};appendFeed(state,'Player '+target+' updated public Smart Priority stops.');break;}
      case 'PRIORITY_WINDOW_OPENED':{const flow=clone(p.flow||{});flow.windowId=String(flow.windowId||('priority_'+event.seq));flow.mode=['relaxed','standard','full'].includes(String(flow.mode||'').toLowerCase())?String(flow.mode).toLowerCase():state.smartPriority.mode;flow.order=Array.isArray(flow.order)?flow.order.map(Number).filter(Boolean):[];flow.passed=Array.isArray(flow.passed)?flow.passed.map(Number).filter(Boolean):[];flow.holder=flow.holder==null?null:Number(flow.holder);flow.consecutivePassCount=Math.max(0,Number(flow.consecutivePassCount||0));flow.complete=!!flow.complete;state.priorityFlow=flow;state.priority=flow.holder;state.respond=state.priority;break;}
      case 'PRIORITY_PASSED':case 'PRIORITY_CLAIMED':{const flow=clone(p.flow||{});flow.order=Array.isArray(flow.order)?flow.order.map(Number).filter(Boolean):[];flow.passed=Array.isArray(flow.passed)?flow.passed.map(Number).filter(Boolean):[];flow.holder=flow.holder==null?null:Number(flow.holder);flow.consecutivePassCount=Math.max(0,Number(flow.consecutivePassCount||0));flow.complete=!!flow.complete;state.priorityFlow={...(state.priorityFlow||{}),...flow};state.priority=state.priorityFlow.holder;state.respond=state.priority;break;}
      case 'PRIORITY_WINDOW_CLOSED':{state.priorityFlow={windowId:null,mode:state.smartPriority.mode,reason:p.reason||null,sourceStackId:null,order:[],holder:null,passed:[],consecutivePassCount:0,complete:true,closedAtSeq:event.seq};state.priority=null;state.respond=null;break;}
      case 'PRIORITY_SET':{state.priority=p.playerId??null;state.respond=state.priority;state.priorityFlow={...(state.priorityFlow||{}),holder:state.priority,complete:false};break;}
      case 'PLAYER_FALLEN':{const target=Number(p.playerId??id),already=!!state.fallen[target];if(!already){state.fallen[target]={reason:p.reason||'unknown',turn:state.turn||1,at:event.at};applyPlayerLeavesGame(state,target,event,p);appendFeed(state,'Player '+target+' has fallen.');}let g=state.combat?.actionGate;if(g&&!g.complete){g.pending=(g.pending||[]).map(Number).filter(x=>x!==target&&!state.fallen?.[x]);g.completed=(g.completed||[]).map(Number).filter(x=>x!==target);g.complete=g.pending.length===0;}break;}
      case 'PLAYER_RESTORED':{const target=p.playerId??id;if(state.fallen[target]){delete state.fallen[target];appendFeed(state,'Player '+target+' status was restored after correction; departed tracked game objects were not recreated.');}break;}
      case 'MATCH_FINALIZED':{if(!state.matchFinalization){state.matchFinalization=clone(p);appendFeed(state,'Match finalized. Winner: Player '+p.winnerId+'.');}break;}
      default:throw new Error('Unknown event type: '+event.type);
    }
    state.meta.seq=event.seq;return state;
  }

  function snapshotForUndo(state){const snap=clone({...state,snapshots:[],events:state.events||[]});state.snapshots.push(snap);if(state.snapshots.length>SNAPSHOT_LIMIT)state.snapshots.shift();}
  function dispatch(state,type,payload,actorId,meta={}){normalizeState(state);snapshotForUndo(state);const event=makeEvent(state,type,payload,actorId,meta);if(typeof MTGLKI!=='undefined')MTGLKI.prepareEvent(state,event,meta);reduce(state,event);if(typeof MTGLKI!=='undefined')MTGLKI.finalizeEvent(state,event);state.events.push(event);if(state.events.length>EVENT_LIMIT)state.events.splice(0,state.events.length-EVENT_LIMIT);return event;}
  function undo(state){normalizeState(state);if(!state.snapshots.length)return null;const snapshots=state.snapshots.slice(0,-1),prior=state.snapshots[state.snapshots.length-1],restored=clone(prior);restored.snapshots=snapshots;normalizeState(restored);return restored;}
  function rollbackToSequence(state,seq){normalizeState(state);seq=Number(seq);if(!Number.isInteger(seq)||seq<0)return null;if(seq===(state.meta.seq||0))return clone(state);let i=state.snapshots.map(x=>Number(x?.meta?.seq||0)).lastIndexOf(seq);if(i<0)return null;let restored=clone(state.snapshots[i]);restored.snapshots=state.snapshots.slice(0,i+1).map(clone);normalizeState(restored);return restored;}

  function commanderDamageEntries(state,defenderId){normalizeState(state);return Object.entries(state.cmd[defenderId]||{}).map(([sourceId,damage])=>({sourceId,damage:Number(damage)||0}));}
  function maxCommanderDamage(state,playerId){return Math.max(0,...commanderDamageEntries(state,playerId).map(x=>x.damage));}
  function lossReason(state,playerId){normalizeState(state);if(asBigInt(state.life[playerId]??40)<=0n)return 'life';if((state.poison[playerId]||0)>=10)return 'poison';if(maxCommanderDamage(state,playerId)>=21)return 'commander';return null;}

  function explainTokenMath(baseQuantity,modifiers=[]){let value=asBigInt(baseQuantity,1n),steps=['Base tokens: '+formatExact(value)];for(const m of modifiers){let mult=asBigInt(m.multiplier,1n);value*=mult;steps.push((m.label||'Multiplier')+': ×'+formatExact(mult)+' → '+formatExact(value));}return{title:'Token calculation',steps,result:formatExact(value)+' tokens',value:value.toString()};}
  function explainTriggerMath(baseCount,modifiers=[]){let value=asBigInt(baseCount,1n),steps=['Base triggers: '+formatExact(value)];for(const m of modifiers){let extra=asBigInt(m.extra,0n);value+=extra;steps.push((m.label||'Additional triggers')+': +'+formatExact(extra)+' → '+formatExact(value));}return{title:'Trigger calculation',steps,result:formatExact(value)+' triggers',value:value.toString()};}
  function explainEvent(state,eventOrId){
    normalizeState(state);let e=typeof eventOrId==='object'?eventOrId:state.events.find(x=>x.id===eventOrId||x.seq===Number(eventOrId));if(!e)return null;
    let calc=state.calculations.find(c=>c.eventId===e.id);if(calc)return clone(calc);
    const p=e.payload||{};
    if(e.type==='COMMANDER_CAST_RECORDED'){const pid=p.playerId??e.actorId,cid=String(p.commanderId||commanderKey(pid)),ce=state.commanderCasting?.byPlayer?.[String(pid)]?.[cid],casts=ce?.castsFromCommand??0;return{title:'Commander tax',steps:['Command-zone casts for this commander: '+casts,'Tax for the next command-zone cast: casts × 2'],result:'Next commander tax: '+Math.max(0,Number(casts)*2)};}
    if(e.type==='COUNTER_CHANGED')return{title:'Counter change',steps:['Counter: '+p.counter,'Change: '+(p.delta>=0?'+':'')+p.delta],result:'Applied to Player '+(p.playerId??e.actorId)};
    if(e.type==='PERMANENT_COUNTER_CHANGED')return{title:'Permanent counter change',steps:['Object: '+p.objectId,'Counter: '+(p.counter||'+1/+1'),'Change: '+String(p.delta||1)],result:'Applied exactly once as an authoritative event'};
    return{title:e.type.replaceAll('_',' '),steps:['Event #'+e.seq,'Source: '+(e.source||'unknown'),'Actor: '+(e.actorId??'system')],result:'Authoritative state event'};
  }

  return {VERSION,normalizeState,normalizeStackItem,registerCommander,commanderKey,commanderDamageEntries,maxCommanderDamage,makeEvent,reduce,dispatch,undo,rollbackToSequence,lossReason,applyPlayerLeavesGame,clone,asBigInt,formatExact,exactAdd,explainTokenMath,explainTriggerMath,explainEvent};
});

